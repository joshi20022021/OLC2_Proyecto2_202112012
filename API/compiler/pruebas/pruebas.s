.data
float_0: .double 3.14
str_1: .asciz "Hola!"
float_2: .double 0
str_3: .asciz ""
float_4: .double 2.71
str_5: .asciz "Adios!"
float_6: .double 15
float_7: .double 16
float_8: .double 15.5
float_9: .double 15.5
float_10: .double 11
float_11: .double 12.5
float_12: .double 11
float_13: .double 5
float_14: .double 2.5
float_15: .double 2.5
float_16: .double 2.5
lit_18: .asciz "\=\=\= Archivo de prueba básico \=\=\="
newline_18: .asciz "\n"
lit_20: .asciz "Validaciones manuales esperadas: 4"
newline_20: .asciz "\n"
lit_22: .asciz "\=\=\=\= Declaración de variables \=\=\=\="
newline_22: .asciz "\n"
lit_24: .asciz "Declaración explícita con tipo y valor"
newline_24: .asciz "\n"
lit_26: .asciz "\n\n###Validacion Manual"
newline_26: .asciz "\n"
lit_28: .asciz "entero:"
space_29: .asciz " "
int_buffer_29: .space 20
newline_30: .asciz "\n"
lit_32: .asciz "decimal:"
space_33: .asciz " "
float_buffer_33: .space 30
newline_34: .asciz "\n"
lit_36: .asciz "texto:"
space_37: .asciz " "
empty_str_37: .asciz ""
newline_38: .asciz "\n"
lit_40: .asciz "booleano:"
space_41: .asciz " "
true_str_42: .asciz "true"
false_str_43: .asciz "false"
newline_43: .asciz "\n"
lit_45: .asciz "caracter:"
space_46: .asciz " "
newline_46: .asciz "\n"
lit_48: .asciz ""
newline_48: .asciz "\n"
lit_50: .asciz "Declaración sin valor"
newline_50: .asciz "\n"
lit_52: .asciz "enteroSinValor:"
space_53: .asciz " "
int_buffer_53: .space 20
newline_54: .asciz "\n"
lit_56: .asciz "decimalSinValor:"
space_57: .asciz " "
float_buffer_57: .space 30
newline_58: .asciz "\n"
lit_60: .asciz "textoSinValor:"
space_61: .asciz " "
empty_str_61: .asciz ""
newline_62: .asciz "\n"
lit_64: .asciz "booleanoSinValor:"
space_65: .asciz " "
true_str_66: .asciz "true"
false_str_67: .asciz "false"
newline_67: .asciz "\n"
lit_69: .asciz ""
newline_69: .asciz "\n"
lit_71: .asciz "Declaración con inferencia de tipo"
newline_71: .asciz "\n"
lit_73: .asciz "enteroInferido:"
space_74: .asciz " "
int_buffer_74: .space 20
newline_75: .asciz "\n"
lit_77: .asciz "decimalInferido:"
space_78: .asciz " "
float_buffer_78: .space 30
newline_79: .asciz "\n"
lit_81: .asciz "textoInferido:"
space_82: .asciz " "
empty_str_82: .asciz ""
newline_83: .asciz "\n"
lit_85: .asciz "booleanoInferido:"
space_86: .asciz " "
true_str_87: .asciz "true"
false_str_88: .asciz "false"
newline_88: .asciz "\n"
lit_90: .asciz "\n\=\=\=\= Asignación de variables \=\=\=\="
newline_90: .asciz "\n"
lit_92: .asciz "Asignación con tipo correcto"
newline_92: .asciz "\n"
float_93: .double 9.9
str_94: .asciz "Nuevo"
lit_96: .asciz "\n\n###Validacion Manual"
newline_96: .asciz "\n"
lit_98: .asciz "entero:"
space_99: .asciz " "
int_buffer_99: .space 20
newline_100: .asciz "\n"
lit_102: .asciz "decimal:"
space_103: .asciz " "
float_buffer_103: .space 30
newline_104: .asciz "\n"
lit_106: .asciz "texto:"
space_107: .asciz " "
empty_str_107: .asciz ""
newline_108: .asciz "\n"
lit_110: .asciz "booleano:"
space_111: .asciz " "
true_str_112: .asciz "true"
false_str_113: .asciz "false"
newline_113: .asciz "\n"
lit_115: .asciz ""
newline_115: .asciz "\n"
lit_117: .asciz "Asignación con expresiones"
newline_117: .asciz "\n"
float_118: .double 19.8
str_119: .asciz "Nuevo!"
lit_121: .asciz "entero:"
space_122: .asciz " "
int_buffer_122: .space 20
newline_123: .asciz "\n"
lit_125: .asciz "decimal:"
space_126: .asciz " "
float_buffer_126: .space 30
newline_127: .asciz "\n"
lit_129: .asciz "texto:"
space_130: .asciz " "
empty_str_130: .asciz ""
newline_131: .asciz "\n"
lit_133: .asciz "booleano:"
space_134: .asciz " "
true_str_135: .asciz "true"
false_str_136: .asciz "false"
newline_136: .asciz "\n"
lit_138: .asciz ""
newline_138: .asciz "\n"
lit_140: .asciz "Suma"
newline_140: .asciz "\n"
lit_142: .asciz "10 + 5 \="
space_143: .asciz " "
float_buffer_143: .space 30
newline_144: .asciz "\n"
lit_146: .asciz "10.5 + 5.5 \="
space_147: .asciz " "
float_buffer_147: .space 30
newline_148: .asciz "\n"
lit_150: .asciz "10 + 5.5 \="
space_151: .asciz " "
float_buffer_151: .space 30
newline_152: .asciz "\n"
lit_154: .asciz "10.5 + 5 \="
space_155: .asciz " "
float_buffer_155: .space 30
newline_156: .asciz "\n"
lit_158: .asciz ""
newline_158: .asciz "\n"
lit_160: .asciz "Multiplicación"
newline_160: .asciz "\n"
lit_162: .asciz "5 * 3 \="
space_163: .asciz " "
int_buffer_163: .space 20
newline_164: .asciz "\n"
lit_166: .asciz "5.5 * 2.0 \="
space_167: .asciz " "
float_buffer_167: .space 30
newline_168: .asciz "\n"
lit_170: .asciz "5 * 2.5 \="
space_171: .asciz " "
float_buffer_171: .space 30
newline_172: .asciz "\n"
lit_174: .asciz "5.5 * 2 \="
space_175: .asciz " "
float_buffer_175: .space 30
newline_176: .asciz "\n"
lit_178: .asciz ""
newline_178: .asciz "\n"
lit_180: .asciz "División"
newline_180: .asciz "\n"
lit_182: .asciz "10 / 2 \="
space_183: .asciz " "
float_buffer_183: .space 30
newline_184: .asciz "\n"
lit_186: .asciz "10.0 / 4.0 \="
space_187: .asciz " "
float_buffer_187: .space 30
newline_188: .asciz "\n"
lit_190: .asciz "10 / 4.0 \="
space_191: .asciz " "
float_buffer_191: .space 30
newline_192: .asciz "\n"
lit_194: .asciz "10.0 / 4 \="
space_195: .asciz " "
float_buffer_195: .space 30
newline_196: .asciz "\n"

// ARM64 Assembly
.text
.global _start
_start:
    stp x29, x30, [sp, #-16]!
    mov x29, sp
    sub sp, sp, #512

    // Declaraciones de variables
    // Debug: Símbolos en tabla: 28
    // Símbolo: '812', Tipo: 'Función', Dato: 'void'

    // Declaración de variable: main
    // Símbolo: '815', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosDeclaracion
    mov x9, #0
    str x9, [sp, #16]
    // Símbolo: '818', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: entero
    mov x9, #42
    str x9, [sp, #24]
    // Símbolo: '821', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: decimal
    adr x9, float_0
    ldr d0, [x9]
    str d0, [sp, #32]
    // Símbolo: '824', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: texto
    adr x9, str_1
    str x9, [sp, #40]
    // Símbolo: '827', Tipo: 'Variable', Dato: 'bool'

    // Declaración de variable: booleano
    mov x9, #1
    str x9, [sp, #48]
    // Símbolo: '830', Tipo: 'Variable', Dato: 'rune'

    // Declaración de variable: caracter
    mov w9, #65
    str w9, [sp, #56]
    // Símbolo: '833', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: enteroSinValor
    mov x9, #0
    str x9, [sp, #64]
    // Símbolo: '836', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: decimalSinValor
    adr x9, float_2
    ldr d0, [x9]
    str d0, [sp, #72]
    // Símbolo: '839', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: textoSinValor
    adr x9, str_3
    str x9, [sp, #80]
    // Símbolo: '842', Tipo: 'Variable', Dato: 'bool'

    // Declaración de variable: booleanoSinValor
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '845', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: enteroInferido
    mov x9, #100
    str x9, [sp, #96]
    // Símbolo: '848', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: decimalInferido
    adr x9, float_4
    ldr d0, [x9]
    str d0, [sp, #104]
    // Símbolo: '851', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: textoInferido
    adr x9, str_5
    str x9, [sp, #112]
    // Símbolo: '854', Tipo: 'Variable', Dato: 'bool'

    // Declaración de variable: booleanoInferido
    mov x9, #0
    str x9, [sp, #120]
    // Símbolo: '857', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosAsignacion
    mov x9, #0
    str x9, [sp, #128]
    // Símbolo: '860', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoSuma1
    adr x9, float_6
    ldr d0, [x9]
    str d0, [sp, #136]
    // Símbolo: '863', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoSuma2
    adr x9, float_7
    ldr d0, [x9]
    str d0, [sp, #144]
    // Símbolo: '866', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoSuma3
    adr x9, float_8
    ldr d0, [x9]
    str d0, [sp, #152]
    // Símbolo: '869', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoSuma4
    adr x9, float_9
    ldr d0, [x9]
    str d0, [sp, #160]
    // Símbolo: '872', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: resultadoMult1
    mov x9, #15
    str x9, [sp, #168]
    // Símbolo: '875', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoMult2
    adr x9, float_10
    ldr d0, [x9]
    str d0, [sp, #176]
    // Símbolo: '878', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoMult3
    adr x9, float_11
    ldr d0, [x9]
    str d0, [sp, #184]
    // Símbolo: '881', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoMult4
    adr x9, float_12
    ldr d0, [x9]
    str d0, [sp, #192]
    // Símbolo: '884', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoDiv1
    adr x9, float_13
    ldr d0, [x9]
    str d0, [sp, #200]
    // Símbolo: '887', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoDiv2
    adr x9, float_14
    ldr d0, [x9]
    str d0, [sp, #208]
    // Símbolo: '890', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoDiv3
    adr x9, float_15
    ldr d0, [x9]
    str d0, [sp, #216]
    // Símbolo: '893', Tipo: 'Variable', Dato: 'float64'

    // Declaración de variable: resultadoDiv4
    adr x9, float_16
    ldr d0, [x9]
    str d0, [sp, #224]

    // Procesando instrucciones

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \=\=\= Archivo de prueba básico \=\=\=
    adr x0, lit_18
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_18
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_18
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_18
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Validaciones manuales esperadas: 4
    adr x0, lit_20
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_20
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_20
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_20
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \=\=\=\= Declaración de variables \=\=\=\=
    adr x0, lit_22
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_22
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_22
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_22
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Declaración explícita con tipo y valor
    adr x0, lit_24
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_24
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_24
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_24
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_26
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_26
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_26
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_26
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: entero:
    adr x0, lit_28
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_28
    mov x8, #64
    svc #0
    adr x0, space_29
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_29
    mov x8, #64
    svc #0
    // Imprimir entero: entero
    ldr x0, [sp, #24]
    adr x1, int_buffer_29
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_29
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_30
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_30
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: decimal:
    adr x0, lit_32
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_32
    mov x8, #64
    svc #0
    adr x0, space_33
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_33
    mov x8, #64
    svc #0
    // Imprimir flotante: decimal
    ldr d0, [sp, #32]
    adr x0, float_buffer_33
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_33
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_34
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_34
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: texto:
    adr x0, lit_36
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_36
    mov x8, #64
    svc #0
    adr x0, space_37
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_37
    mov x8, #64
    svc #0
    // Imprimir string: texto
    ldr x0, [sp, #40]
    cmp x0, #0
    beq skip_print_37
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #40]
    mov x8, #64
    svc #0
    b end_print_37
skip_print_37:
    adr x0, empty_str_37
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_37
    mov x8, #64
    svc #0
end_print_37:
    // Imprimir salto de línea
    adr x0, newline_38
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_38
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: booleano:
    adr x0, lit_40
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_40
    mov x8, #64
    svc #0
    adr x0, space_41
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_41
    mov x8, #64
    svc #0
    // Imprimir booleano: booleano
    ldr x0, [sp, #48]
    cmp x0, #0
    beq print_false_43
    mov x0, #1
    adr x1, true_str_42
    mov x2, #4
    mov x8, #64
    svc #0
    b end_print_bool_43
print_false_43:
    mov x0, #1
    adr x1, false_str_43
    mov x2, #5
    mov x8, #64
    svc #0
end_print_bool_43:
    // Imprimir salto de línea
    adr x0, newline_43
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_43
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: caracter:
    adr x0, lit_45
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_45
    mov x8, #64
    svc #0
    adr x0, space_46
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_46
    mov x8, #64
    svc #0
    // Imprimir rune: caracter
    mov x0, #1
    sub sp, sp, #1
    ldr w1, [sp, #56]
    strb w1, [sp]
    mov x1, sp
    mov x2, #1
    mov x8, #64
    svc #0
    add sp, sp, #1
    // Imprimir salto de línea
    adr x0, newline_46
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_46
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_48
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_48
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_48
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_48
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Declaración sin valor
    adr x0, lit_50
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_50
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_50
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_50
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: enteroSinValor:
    adr x0, lit_52
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_52
    mov x8, #64
    svc #0
    adr x0, space_53
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_53
    mov x8, #64
    svc #0
    // Imprimir entero: enteroSinValor
    ldr x0, [sp, #64]
    adr x1, int_buffer_53
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_53
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_54
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_54
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: decimalSinValor:
    adr x0, lit_56
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_56
    mov x8, #64
    svc #0
    adr x0, space_57
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_57
    mov x8, #64
    svc #0
    // Imprimir flotante: decimalSinValor
    ldr d0, [sp, #72]
    adr x0, float_buffer_57
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_57
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_58
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_58
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: textoSinValor:
    adr x0, lit_60
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_60
    mov x8, #64
    svc #0
    adr x0, space_61
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_61
    mov x8, #64
    svc #0
    // Imprimir string: textoSinValor
    ldr x0, [sp, #80]
    cmp x0, #0
    beq skip_print_61
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #80]
    mov x8, #64
    svc #0
    b end_print_61
skip_print_61:
    adr x0, empty_str_61
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_61
    mov x8, #64
    svc #0
end_print_61:
    // Imprimir salto de línea
    adr x0, newline_62
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_62
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: booleanoSinValor:
    adr x0, lit_64
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_64
    mov x8, #64
    svc #0
    adr x0, space_65
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_65
    mov x8, #64
    svc #0
    // Imprimir booleano: booleanoSinValor
    ldr x0, [sp, #88]
    cmp x0, #0
    beq print_false_67
    mov x0, #1
    adr x1, true_str_66
    mov x2, #4
    mov x8, #64
    svc #0
    b end_print_bool_67
print_false_67:
    mov x0, #1
    adr x1, false_str_67
    mov x2, #5
    mov x8, #64
    svc #0
end_print_bool_67:
    // Imprimir salto de línea
    adr x0, newline_67
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_67
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_69
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_69
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_69
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_69
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Declaración con inferencia de tipo
    adr x0, lit_71
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_71
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_71
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_71
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: enteroInferido:
    adr x0, lit_73
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_73
    mov x8, #64
    svc #0
    adr x0, space_74
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_74
    mov x8, #64
    svc #0
    // Imprimir entero: enteroInferido
    ldr x0, [sp, #96]
    adr x1, int_buffer_74
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_74
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_75
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_75
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: decimalInferido:
    adr x0, lit_77
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_77
    mov x8, #64
    svc #0
    adr x0, space_78
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_78
    mov x8, #64
    svc #0
    // Imprimir flotante: decimalInferido
    ldr d0, [sp, #104]
    adr x0, float_buffer_78
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_78
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_79
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_79
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: textoInferido:
    adr x0, lit_81
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_81
    mov x8, #64
    svc #0
    adr x0, space_82
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_82
    mov x8, #64
    svc #0
    // Imprimir string: textoInferido
    ldr x0, [sp, #112]
    cmp x0, #0
    beq skip_print_82
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #112]
    mov x8, #64
    svc #0
    b end_print_82
skip_print_82:
    adr x0, empty_str_82
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_82
    mov x8, #64
    svc #0
end_print_82:
    // Imprimir salto de línea
    adr x0, newline_83
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_83
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: booleanoInferido:
    adr x0, lit_85
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_85
    mov x8, #64
    svc #0
    adr x0, space_86
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_86
    mov x8, #64
    svc #0
    // Imprimir booleano: booleanoInferido
    ldr x0, [sp, #120]
    cmp x0, #0
    beq print_false_88
    mov x0, #1
    adr x1, true_str_87
    mov x2, #4
    mov x8, #64
    svc #0
    b end_print_bool_88
print_false_88:
    mov x0, #1
    adr x1, false_str_88
    mov x2, #5
    mov x8, #64
    svc #0
end_print_bool_88:
    // Imprimir salto de línea
    adr x0, newline_88
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_88
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= Asignación de variables \=\=\=\=
    adr x0, lit_90
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_90
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_90
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_90
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Asignación con tipo correcto
    adr x0, lit_92
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_92
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_92
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_92
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 99 a entero (int) en [sp, #24]
    mov x9, #99
    str x9, [sp, #24]

    // Procesando nodo: Asignacion  Valor=
    // Asignando 9,9 a decimal (float64) en [sp, #32]
    adr x9, float_93
    ldr d0, [x9]
    str d0, [sp, #32]

    // Procesando nodo: Asignacion  Valor=
    // Asignando Nuevo a texto (string) en [sp, #40]
    adr x0, str_94
    str x0, [sp, #40]

    // Procesando nodo: Negacion  Valor=!

    // Procesando nodo: Asignacion  Valor=
    // Asignando False a booleano (bool) en [sp, #48]
    mov x0, #0
    str x0, [sp, #48]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_96
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_96
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_96
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_96
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: entero:
    adr x0, lit_98
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_98
    mov x8, #64
    svc #0
    adr x0, space_99
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_99
    mov x8, #64
    svc #0
    // Imprimir entero: entero
    ldr x0, [sp, #24]
    adr x1, int_buffer_99
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_99
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_100
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_100
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: decimal:
    adr x0, lit_102
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_102
    mov x8, #64
    svc #0
    adr x0, space_103
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_103
    mov x8, #64
    svc #0
    // Imprimir flotante: decimal
    ldr d0, [sp, #32]
    adr x0, float_buffer_103
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_103
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_104
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_104
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: texto:
    adr x0, lit_106
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_106
    mov x8, #64
    svc #0
    adr x0, space_107
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_107
    mov x8, #64
    svc #0
    // Imprimir string: texto
    ldr x0, [sp, #40]
    cmp x0, #0
    beq skip_print_107
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #40]
    mov x8, #64
    svc #0
    b end_print_107
skip_print_107:
    adr x0, empty_str_107
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_107
    mov x8, #64
    svc #0
end_print_107:
    // Imprimir salto de línea
    adr x0, newline_108
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_108
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: booleano:
    adr x0, lit_110
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_110
    mov x8, #64
    svc #0
    adr x0, space_111
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_111
    mov x8, #64
    svc #0
    // Imprimir booleano: booleano
    ldr x0, [sp, #48]
    cmp x0, #0
    beq print_false_113
    mov x0, #1
    adr x1, true_str_112
    mov x2, #4
    mov x8, #64
    svc #0
    b end_print_bool_113
print_false_113:
    mov x0, #1
    adr x1, false_str_113
    mov x2, #5
    mov x8, #64
    svc #0
end_print_bool_113:
    // Imprimir salto de línea
    adr x0, newline_113
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_113
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_115
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_115
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_115
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_115
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Asignación con expresiones
    adr x0, lit_117
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_117
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_117
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_117
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 100 a entero (int) en [sp, #24]
    mov x9, #100
    str x9, [sp, #24]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 19,8 a decimal (float64) en [sp, #32]
    adr x9, float_118
    ldr d0, [x9]
    str d0, [sp, #32]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando Nuevo! a texto (string) en [sp, #40]
    adr x0, str_119
    str x0, [sp, #40]

    // Procesando nodo: Negacion  Valor=!

    // Procesando nodo: Asignacion  Valor=
    // Asignando True a booleano (bool) en [sp, #48]
    mov x0, #1
    str x0, [sp, #48]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: entero:
    adr x0, lit_121
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_121
    mov x8, #64
    svc #0
    adr x0, space_122
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_122
    mov x8, #64
    svc #0
    // Imprimir entero: entero
    ldr x0, [sp, #24]
    adr x1, int_buffer_122
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_122
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_123
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_123
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: decimal:
    adr x0, lit_125
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_125
    mov x8, #64
    svc #0
    adr x0, space_126
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_126
    mov x8, #64
    svc #0
    // Imprimir flotante: decimal
    ldr d0, [sp, #32]
    adr x0, float_buffer_126
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_126
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_127
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_127
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: texto:
    adr x0, lit_129
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_129
    mov x8, #64
    svc #0
    adr x0, space_130
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_130
    mov x8, #64
    svc #0
    // Imprimir string: texto
    ldr x0, [sp, #40]
    cmp x0, #0
    beq skip_print_130
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #40]
    mov x8, #64
    svc #0
    b end_print_130
skip_print_130:
    adr x0, empty_str_130
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_130
    mov x8, #64
    svc #0
end_print_130:
    // Imprimir salto de línea
    adr x0, newline_131
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_131
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: booleano:
    adr x0, lit_133
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_133
    mov x8, #64
    svc #0
    adr x0, space_134
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_134
    mov x8, #64
    svc #0
    // Imprimir booleano: booleano
    ldr x0, [sp, #48]
    cmp x0, #0
    beq print_false_136
    mov x0, #1
    adr x1, true_str_135
    mov x2, #4
    mov x8, #64
    svc #0
    b end_print_bool_136
print_false_136:
    mov x0, #1
    adr x1, false_str_136
    mov x2, #5
    mov x8, #64
    svc #0
end_print_bool_136:
    // Imprimir salto de línea
    adr x0, newline_136
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_136
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_138
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_138
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_138
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_138
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Suma
    adr x0, lit_140
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_140
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_140
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_140
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10 + 5 \=
    adr x0, lit_142
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_142
    mov x8, #64
    svc #0
    adr x0, space_143
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_143
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoSuma1
    ldr d0, [sp, #136]
    adr x0, float_buffer_143
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_143
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_144
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_144
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10.5 + 5.5 \=
    adr x0, lit_146
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_146
    mov x8, #64
    svc #0
    adr x0, space_147
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_147
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoSuma2
    ldr d0, [sp, #144]
    adr x0, float_buffer_147
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_147
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_148
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_148
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10 + 5.5 \=
    adr x0, lit_150
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_150
    mov x8, #64
    svc #0
    adr x0, space_151
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_151
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoSuma3
    ldr d0, [sp, #152]
    adr x0, float_buffer_151
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_151
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_152
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_152
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10.5 + 5 \=
    adr x0, lit_154
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_154
    mov x8, #64
    svc #0
    adr x0, space_155
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_155
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoSuma4
    ldr d0, [sp, #160]
    adr x0, float_buffer_155
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_155
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_156
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_156
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_158
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_158
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_158
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_158
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Multiplicación
    adr x0, lit_160
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_160
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_160
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_160
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 5 * 3 \=
    adr x0, lit_162
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_162
    mov x8, #64
    svc #0
    adr x0, space_163
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_163
    mov x8, #64
    svc #0
    // Imprimir entero: resultadoMult1
    ldr x0, [sp, #168]
    adr x1, int_buffer_163
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_163
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_164
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_164
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 5.5 * 2.0 \=
    adr x0, lit_166
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_166
    mov x8, #64
    svc #0
    adr x0, space_167
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_167
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoMult2
    ldr d0, [sp, #176]
    adr x0, float_buffer_167
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_167
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_168
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_168
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 5 * 2.5 \=
    adr x0, lit_170
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_170
    mov x8, #64
    svc #0
    adr x0, space_171
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_171
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoMult3
    ldr d0, [sp, #184]
    adr x0, float_buffer_171
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_171
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_172
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_172
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 5.5 * 2 \=
    adr x0, lit_174
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_174
    mov x8, #64
    svc #0
    adr x0, space_175
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_175
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoMult4
    ldr d0, [sp, #192]
    adr x0, float_buffer_175
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_175
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_176
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_176
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_178
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_178
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_178
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_178
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: División
    adr x0, lit_180
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_180
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_180
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_180
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=/

    // Procesando nodo: Operacion  Valor=/

    // Procesando nodo: Operacion  Valor=/

    // Procesando nodo: Operacion  Valor=/

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10 / 2 \=
    adr x0, lit_182
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_182
    mov x8, #64
    svc #0
    adr x0, space_183
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_183
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoDiv1
    ldr d0, [sp, #200]
    adr x0, float_buffer_183
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_183
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_184
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_184
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10.0 / 4.0 \=
    adr x0, lit_186
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_186
    mov x8, #64
    svc #0
    adr x0, space_187
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_187
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoDiv2
    ldr d0, [sp, #208]
    adr x0, float_buffer_187
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_187
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_188
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_188
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10 / 4.0 \=
    adr x0, lit_190
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_190
    mov x8, #64
    svc #0
    adr x0, space_191
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_191
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoDiv3
    ldr d0, [sp, #216]
    adr x0, float_buffer_191
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_191
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_192
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_192
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 10.0 / 4 \=
    adr x0, lit_194
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_194
    mov x8, #64
    svc #0
    adr x0, space_195
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_195
    mov x8, #64
    svc #0
    // Imprimir flotante: resultadoDiv4
    ldr d0, [sp, #224]
    adr x0, float_buffer_195
    bl float_to_string
    mov x2, x0
    mov x0, #1
    adr x1, float_buffer_195
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_196
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_196
    mov x8, #64
    svc #0

    // Saltar directamente al final del programa
    b program_exit

// Rutinas auxiliares

string_copy:
    // x0 = destino, x1 = origen
    cmp x1, #0
    beq string_copy_null
    mov x2, x0
string_copy_loop:
    ldrb w3, [x1], #1
    strb w3, [x0], #1
    cmp w3, #0
    bne string_copy_loop
    sub x0, x0, #1
    ret
string_copy_null:
    // Si origen es NULL, simplemente poner terminador nulo en destino
    mov w3, #0
    strb w3, [x0]
    ret
int_to_string:
    // x0 = número, x1 = buffer
    mov x9, x0        // Guardar número original
    mov x10, x1       // Guardar dirección original del buffer
    mov x11, #0       // Contador de dígitos
    mov x12, #0       // Para contar dígitos totales
    mov x14, #0       // Flag para indicar si es negativo
    cmp x9, #0
    bne count_digits
    mov w2, #48       // '0'
    strb w2, [x10]
    mov w2, #0        // Null-terminator
    strb w2, [x10, #1]
    mov x0, #1
    ret
    // Verificar si es negativo
    cmp x9, #0
    bge positive_number
    // Es negativo, imprimir '-'
    neg x9, x9     // Convertir a positivo
    mov w2, #45    // '-' en ASCII
    strb w2, [x10], #1
    mov x14, #1    // Flag para indicar que es negativo (afectará a la longitud final)
positive_number:
count_digits:
    mov x2, x9        // Copiar número
    mov x13, #10      // Constante 10 para división
digit_count_loop:
    cbz x2, gen_digits
    udiv x2, x2, x13  // Dividir por 10 usando registro
    add x12, x12, #1  // Incrementar contador
    b digit_count_loop
gen_digits:
    mov x11, x12      // Longitud total
    mov x2, #1        // Potencia de 10
    mov x13, #10      // Base 10
calc_power_loop:
    cmp x12, #1       // Si solo queda 1 dígito, salir
    ble extract_digits
    mul x2, x2, x13   // Multiplicar por 10
    sub x12, x12, #1  // Decrementar contador
    b calc_power_loop
extract_digits:
    mov x3, x9        // Restaurar número
    mov x12, x11      // Restaurar contador total
extract_loop:
    cbz x12, finish_str
    udiv x4, x3, x2   // x4 = número / potencia
    msub x3, x4, x2, x3 // x3 = x3 - (x4 * x2) (resto)
    add w4, w4, #48   // Convertir a ASCII
    strb w4, [x10], #1// Almacenar y avanzar
    udiv x2, x2, x13  // Reducir potencia por 10 (x13=10)
    sub x12, x12, #1  // Decrementar contador
    b extract_loop
finish_str:
    mov w2, #0        // Null-terminator
    strb w2, [x10]
    // Si era negativo, incrementar longitud por el signo
    cbz x14, return_length
    add x11, x11, #1  // Ajustar longitud por el signo '-'
return_length:
    mov x0, x11       // Retornar longitud
    ret

string_length:
    // x0 = dirección de la cadena
    cmp x0, #0
    beq string_length_null
    mov x2, x0
length_loop:
    ldrb w1, [x0], #1  // cargar byte y avanzar
    cmp w1, #0         // comparar con null
    bne length_loop    // continuar si no es null
    sub x0, x0, x2     // calcular longitud
    sub x0, x0, #1     // ajustar por null
    ret
string_length_null:
    mov x0, #0         // Retornar 0 para cadenas NULL
    ret

float_to_string:
    // Implementación mejorada
    stp x29, x30, [sp, #-16]!
    mov x29, sp
    stp x19, x20, [sp, #-16]!
    mov x19, x0                // Buffer address

    // Extraer signo
    mov x21, #0               // Flag para negativo
    fcmpe d0, #0.0
    bpl convert_integer
    mov x21, #1               // Es negativo
    fneg d0, d0               // Convertir a positivo
    mov w1, #45               // '-'
    strb w1, [x19], #1
convert_integer:
    // Convertir parte entera
    fcvtzs x0, d0             // Convertir a entero
    mov x1, x19               // Buffer para la parte entera
    bl int_to_string          // Convertir entero
    mov x20, x0               // Guardar longitud
    cmp x21, #1               // Si era negativo
    beq was_negative
    add x19, x19, x0          // Avanzar buffer
    b add_decimal
was_negative:
    add x19, x19, x0          // Avanzar buffer
    add x20, x20, #1          // +1 por el signo
add_decimal:
    // Añadir punto
    mov w1, #'.'
    strb w1, [x19], #1
    add x20, x20, #1

    // Convertir 6 dígitos decimales
    fcvtzs x0, d0             // Volver a convertir a entero
    scvtf d1, x0              // Convertir entero a float
    fsub d1, d0, d1           // Parte decimal = float - entero
    mov x22, #2               // 6 dígitos decimales
    fmov d2, #10.0            // Constante 10.0
decimal_loop:
    fmul d1, d1, d2           // d1 = d1 * 10
    fcvtzs x0, d1             // Entero = int(d1)
    add w0, w0, #48           // Convertir a ASCII
    strb w0, [x19], #1        // Guardar y avanzar
    add x20, x20, #1          // Incrementar longitud
    scvtf d3, x0              // Convertir dígito a flotante
    mov x0, #48               // Valor ASCII '0'
    scvtf d4, x0              // Convertir 48 a float
    fsub d3, d3, d4           // Restar ASCII offset
    fsub d1, d1, d3           // Restar dígito
    subs x22, x22, #1         // Decrementar contador
    bne decimal_loop          // Continuar si no hemos terminado
    // Terminar cadena
    mov w1, #0
    strb w1, [x19]

    // Retornar longitud total
    mov x0, x20
    ldp x19, x20, [sp], #16
    ldp x29, x30, [sp], #16
    ret

program_exit:
    // Terminar programa
    mov sp, x29
    ldp x29, x30, [sp], #16
    mov x8, #93     // exit syscall
    mov x0, #0      // exit code 0
    svc #0          // supervisor call
    