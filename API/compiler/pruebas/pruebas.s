.data
str_0: .asciz ""
str_1: .asciz ""
str_2: .asciz ""
str_3: .asciz ""
str_4: .asciz ""
lit_6: .asciz "\=\=\= Archivo de prueba de funcionalidades intermedias \=\=\="
newline_6: .asciz "\n"
lit_8: .asciz "Validaciones manuales esperadas: 4"
newline_8: .asciz "\n"
lit_10: .asciz "\=\=\=\= Manejo de entornos \=\=\=\="
newline_10: .asciz "\n"
lit_12: .asciz "\nVariable redeclarada en un entorno diferente"
newline_12: .asciz "\n"
lit_15: .asciz "b dentro del if \="
space_16: .asciz " "
int_buffer_16: .space 20
newline_17: .asciz "\n"
lit_20: .asciz "OK Redeclaración en entorno diferente: correcto"
newline_20: .asciz "\n"
lit_22: .asciz "b fuera del if \="
space_23: .asciz " "
int_buffer_23: .space 20
newline_24: .asciz "\n"
lit_26: .asciz "\nUso de variable en un entorno superior"
newline_26: .asciz "\n"
lit_29: .asciz "c dentro del if \="
space_30: .asciz " "
int_buffer_30: .space 20
newline_31: .asciz "\n"
lit_33: .asciz "d dentro del if \="
space_34: .asciz " "
int_buffer_34: .space 20
newline_35: .asciz "\n"
lit_37: .asciz "c fuera del if \="
space_38: .asciz " "
int_buffer_38: .space 20
newline_39: .asciz "\n"
lit_41: .asciz "d fuera del if \="
space_42: .asciz " "
int_buffer_42: .space 20
newline_43: .asciz "\n"
lit_46: .asciz "OK Uso de variable en entorno superior: correcto"
newline_46: .asciz "\n"
lit_48: .asciz "\n\=\=\=\= If / Else \=\=\=\="
newline_48: .asciz "\n"
lit_50: .asciz "\n\n###Validacion Manual"
newline_50: .asciz "\n"
lit_52: .asciz "If simple"
newline_52: .asciz "\n"
lit_55: .asciz "Condición verdadera"
newline_55: .asciz "\n"
lit_57: .asciz "\nIf-Else"
newline_57: .asciz "\n"
lit_60: .asciz "Condición verdadera en if-else"
newline_60: .asciz "\n"
lit_63: .asciz "Condición falsa. ejecutando else"
newline_63: .asciz "\n"
lit_65: .asciz "\nIf-ElseIf-Else"
newline_65: .asciz "\n"
lit_68: .asciz "Primera condición verdadera"
newline_68: .asciz "\n"
lit_72: .asciz "Segunda condición verdadera"
newline_72: .asciz "\n"
lit_74: .asciz ""
newline_74: .asciz "\n"
lit_76: .asciz "\n\=\=\=\= For Tipo While \=\=\=\="
newline_76: .asciz "\n"
lit_78: .asciz "For como while simple"
newline_78: .asciz "\n"
lit_80: .asciz "i \="
space_81: .asciz " "
int_buffer_81: .space 20
newline_82: .asciz "\n"
lit_84: .asciz "i \="
space_85: .asciz " "
int_buffer_85: .space 20
newline_86: .asciz "\n"
lit_88: .asciz "i \="
space_89: .asciz " "
int_buffer_89: .space 20
newline_90: .asciz "\n"
lit_92: .asciz "i \="
space_93: .asciz " "
int_buffer_93: .space 20
newline_94: .asciz "\n"
lit_96: .asciz "i \="
space_97: .asciz " "
int_buffer_97: .space 20
newline_98: .asciz "\n"
lit_101: .asciz "OK For como while simple: correcto"
newline_101: .asciz "\n"
lit_103: .asciz "\nFor como while con condición compuesta"
newline_103: .asciz "\n"
lit_105: .asciz "j \="
space_106: .asciz " "
int_buffer_106: .space 20
space_108: .asciz " "
lit_109: .asciz "producto \="
space_110: .asciz " "
int_buffer_110: .space 20
newline_111: .asciz "\n"
lit_113: .asciz "j \="
space_114: .asciz " "
int_buffer_114: .space 20
space_116: .asciz " "
lit_117: .asciz "producto \="
space_118: .asciz " "
int_buffer_118: .space 20
newline_119: .asciz "\n"
lit_121: .asciz "j \="
space_122: .asciz " "
int_buffer_122: .space 20
space_124: .asciz " "
lit_125: .asciz "producto \="
space_126: .asciz " "
int_buffer_126: .space 20
newline_127: .asciz "\n"
lit_129: .asciz "j \="
space_130: .asciz " "
int_buffer_130: .space 20
space_132: .asciz " "
lit_133: .asciz "producto \="
space_134: .asciz " "
int_buffer_134: .space 20
newline_135: .asciz "\n"
lit_137: .asciz "j \="
space_138: .asciz " "
int_buffer_138: .space 20
space_140: .asciz " "
lit_141: .asciz "producto \="
space_142: .asciz " "
int_buffer_142: .space 20
newline_143: .asciz "\n"
lit_146: .asciz "OK For como while con condición compuesta: correcto"
newline_146: .asciz "\n"
lit_148: .asciz "\nFor como while anidado (patrón X)"
newline_148: .asciz "\n"
lit_150: .asciz "\n\n###Validacion Manual"
newline_150: .asciz "\n"
str_152: .asciz "*"
str_154: .asciz "* "
str_156: .asciz "*  "
str_158: .asciz "*   "
str_160: .asciz "*   *"
empty_str_161: .asciz ""
newline_162: .asciz "\n"
str_164: .asciz " "
str_166: .asciz " *"
str_168: .asciz " * "
str_170: .asciz " * *"
str_172: .asciz " * * "
empty_str_173: .asciz ""
newline_174: .asciz "\n"
str_176: .asciz " "
str_178: .asciz "  "
str_180: .asciz "  *"
str_182: .asciz "  * "
str_184: .asciz "  *  "
empty_str_185: .asciz ""
newline_186: .asciz "\n"
str_188: .asciz " "
str_190: .asciz " *"
str_192: .asciz " * "
str_194: .asciz " * *"
str_196: .asciz " * * "
empty_str_197: .asciz ""
newline_198: .asciz "\n"
str_200: .asciz "*"
str_202: .asciz "* "
str_204: .asciz "*  "
str_206: .asciz "*   "
str_208: .asciz "*   *"
empty_str_209: .asciz ""
newline_210: .asciz "\n"
lit_212: .asciz ""
newline_212: .asciz "\n"
lit_215: .asciz "OK For como while anidado: correcto"
newline_215: .asciz "\n"
lit_217: .asciz "\n\=\=\=\= For Clásico \=\=\=\="
newline_217: .asciz "\n"
lit_219: .asciz "For clásico simple"
newline_219: .asciz "\n"
lit_221: .asciz "i \="
space_222: .asciz " "
int_buffer_222: .space 20
newline_223: .asciz "\n"
lit_225: .asciz "i \="
space_226: .asciz " "
int_buffer_226: .space 20
newline_227: .asciz "\n"
lit_229: .asciz "i \="
space_230: .asciz " "
int_buffer_230: .space 20
newline_231: .asciz "\n"
lit_233: .asciz "i \="
space_234: .asciz " "
int_buffer_234: .space 20
newline_235: .asciz "\n"
lit_237: .asciz "i \="
space_238: .asciz " "
int_buffer_238: .space 20
newline_239: .asciz "\n"
lit_242: .asciz "OK For clásico simple: correcto"
newline_242: .asciz "\n"
lit_244: .asciz "\nFor clásico anidado (tabla de multiplicar)"
newline_244: .asciz "\n"
lit_246: .asciz "\n\n###Validacion Manual"
newline_246: .asciz "\n"
int_buffer_247: .space 20
space_249: .asciz " "
int_buffer_249: .space 20
space_251: .asciz " "
int_buffer_251: .space 20
space_253: .asciz " "
lit_254: .asciz "\="
space_255: .asciz " "
lit_256: .asciz "1"
newline_256: .asciz "\n"
int_buffer_257: .space 20
space_259: .asciz " "
int_buffer_259: .space 20
space_261: .asciz " "
int_buffer_261: .space 20
space_263: .asciz " "
lit_264: .asciz "\="
space_265: .asciz " "
lit_266: .asciz "2"
newline_266: .asciz "\n"
int_buffer_267: .space 20
space_269: .asciz " "
int_buffer_269: .space 20
space_271: .asciz " "
int_buffer_271: .space 20
space_273: .asciz " "
lit_274: .asciz "\="
space_275: .asciz " "
lit_276: .asciz "3"
newline_276: .asciz "\n"
newline_277: .asciz "\n"
int_buffer_278: .space 20
space_280: .asciz " "
int_buffer_280: .space 20
space_282: .asciz " "
int_buffer_282: .space 20
space_284: .asciz " "
lit_285: .asciz "\="
space_286: .asciz " "
lit_287: .asciz "2"
newline_287: .asciz "\n"
int_buffer_288: .space 20
space_290: .asciz " "
int_buffer_290: .space 20
space_292: .asciz " "
int_buffer_292: .space 20
space_294: .asciz " "
lit_295: .asciz "\="
space_296: .asciz " "
lit_297: .asciz "4"
newline_297: .asciz "\n"
int_buffer_298: .space 20
space_300: .asciz " "
int_buffer_300: .space 20
space_302: .asciz " "
int_buffer_302: .space 20
space_304: .asciz " "
lit_305: .asciz "\="
space_306: .asciz " "
lit_307: .asciz "6"
newline_307: .asciz "\n"
newline_308: .asciz "\n"
int_buffer_309: .space 20
space_311: .asciz " "
int_buffer_311: .space 20
space_313: .asciz " "
int_buffer_313: .space 20
space_315: .asciz " "
lit_316: .asciz "\="
space_317: .asciz " "
lit_318: .asciz "3"
newline_318: .asciz "\n"
int_buffer_319: .space 20
space_321: .asciz " "
int_buffer_321: .space 20
space_323: .asciz " "
int_buffer_323: .space 20
space_325: .asciz " "
lit_326: .asciz "\="
space_327: .asciz " "
lit_328: .asciz "6"
newline_328: .asciz "\n"
int_buffer_329: .space 20
space_331: .asciz " "
int_buffer_331: .space 20
space_333: .asciz " "
int_buffer_333: .space 20
space_335: .asciz " "
lit_336: .asciz "\="
space_337: .asciz " "
lit_338: .asciz "9"
newline_338: .asciz "\n"
newline_339: .asciz "\n"
lit_341: .asciz ""
newline_341: .asciz "\n"
lit_343: .asciz "OK For clásico anidado: correcto"
newline_343: .asciz "\n"
lit_345: .asciz "\n\=\=\=\= For Range \=\=\=\="
newline_345: .asciz "\n"
lit_347: .asciz "For range con slice"
newline_347: .asciz "\n"
lit_349: .asciz "Índice"
space_350: .asciz " "
int_buffer_350: .space 20
space_352: .asciz " "
lit_353: .asciz "\="
space_354: .asciz " "
lit_355: .asciz "valor"
newline_355: .asciz "\n"
lit_357: .asciz "Índice"
space_358: .asciz " "
int_buffer_358: .space 20
space_360: .asciz " "
lit_361: .asciz "\="
space_362: .asciz " "
lit_363: .asciz "valor"
newline_363: .asciz "\n"
lit_365: .asciz "Índice"
space_366: .asciz " "
int_buffer_366: .space 20
space_368: .asciz " "
lit_369: .asciz "\="
space_370: .asciz " "
lit_371: .asciz "valor"
newline_371: .asciz "\n"
lit_373: .asciz "Índice"
space_374: .asciz " "
int_buffer_374: .space 20
space_376: .asciz " "
lit_377: .asciz "\="
space_378: .asciz " "
lit_379: .asciz "valor"
newline_379: .asciz "\n"
lit_381: .asciz "Índice"
space_382: .asciz " "
int_buffer_382: .space 20
space_384: .asciz " "
lit_385: .asciz "\="
space_386: .asciz " "
lit_387: .asciz "valor"
newline_387: .asciz "\n"
lit_390: .asciz "OK For range con slice: correcto"
newline_390: .asciz "\n"
lit_393: .asciz "OK For range con índices: correcto"
newline_393: .asciz "\n"
lit_395: .asciz "\n\=\=\=\= Switch/Case \=\=\=\="
newline_395: .asciz "\n"
lit_397: .asciz "\n\n###Validacion Manual"
newline_397: .asciz "\n"
lit_399: .asciz "Switch simple"
newline_399: .asciz "\n"
lit_401: .asciz "Lunes"
newline_401: .asciz "\n"
lit_403: .asciz "\nSwitch con default"
newline_403: .asciz "\n"
lit_405: .asciz "Número no reconocido. se ejecuta default"
newline_405: .asciz "\n"
lit_407: .asciz "\nSwitch con break explícito"
newline_407: .asciz "\n"
lit_409: .asciz "Caso 2 - Se ejecuta este y debe detenerse"
newline_409: .asciz "\n"
lit_411: .asciz ""
newline_411: .asciz "\n"
lit_413: .asciz "\n\=\=\=\= Break \=\=\=\="
newline_413: .asciz "\n"
lit_415: .asciz "Break en for infinito"
newline_415: .asciz "\n"
lit_417: .asciz "contador \="
space_418: .asciz " "
int_buffer_418: .space 20
newline_419: .asciz "\n"
lit_422: .asciz "contador \="
space_423: .asciz " "
int_buffer_423: .space 20
newline_424: .asciz "\n"
lit_427: .asciz "contador \="
space_428: .asciz " "
int_buffer_428: .space 20
newline_429: .asciz "\n"
lit_432: .asciz "contador \="
space_433: .asciz " "
int_buffer_433: .space 20
newline_434: .asciz "\n"
lit_437: .asciz "contador \="
space_438: .asciz " "
int_buffer_438: .space 20
newline_439: .asciz "\n"
lit_443: .asciz "OK Break en for infinito: correcto"
newline_443: .asciz "\n"
lit_445: .asciz "\nBreak en for clásico"
newline_445: .asciz "\n"
lit_447: .asciz "i \="
space_448: .asciz " "
int_buffer_448: .space 20
newline_449: .asciz "\n"
lit_452: .asciz "i \="
space_453: .asciz " "
int_buffer_453: .space 20
newline_454: .asciz "\n"
lit_457: .asciz "i \="
space_458: .asciz " "
int_buffer_458: .space 20
newline_459: .asciz "\n"
lit_462: .asciz "i \="
space_463: .asciz " "
int_buffer_463: .space 20
newline_464: .asciz "\n"
lit_467: .asciz "i \="
space_468: .asciz " "
int_buffer_468: .space 20
newline_469: .asciz "\n"
lit_473: .asciz "OK Break en for clásico: correcto"
newline_473: .asciz "\n"
lit_475: .asciz "\n\=\=\=\= Continue \=\=\=\="
newline_475: .asciz "\n"
lit_477: .asciz "Continue en for tipo while"
newline_477: .asciz "\n"
lit_489: .asciz "Números impares:"
space_490: .asciz " "
int_buffer_490: .space 20
newline_491: .asciz "\n"
lit_494: .asciz "X Continue en for tipo while: incorrecto"
newline_494: .asciz "\n"
lit_496: .asciz "\nContinue en for clásico"
newline_496: .asciz "\n"
lit_508: .asciz "Números pares:"
space_509: .asciz " "
int_buffer_509: .space 20
newline_510: .asciz "\n"
lit_513: .asciz "OK Continue en for clásico: correcto"
newline_513: .asciz "\n"
lit_515: .asciz "\n\=\=\= Tabla de Resultados \=\=\="
newline_515: .asciz "\n"
lit_517: .asciz "+--------------------------+--------+-------+"
newline_517: .asciz "\n"
lit_519: .asciz "| Característica           | Puntos | Total |"
newline_519: .asciz "\n"
lit_521: .asciz "+--------------------------+--------+-------+"
newline_521: .asciz "\n"
lit_523: .asciz "| Manejo de entornos       | "
space_524: .asciz " "
int_buffer_524: .space 20
space_526: .asciz " "
lit_527: .asciz "    | 3     |"
newline_527: .asciz "\n"
lit_529: .asciz "| If / Else                | "
space_530: .asciz " "
int_buffer_530: .space 20
space_532: .asciz " "
lit_533: .asciz "    | 3     |"
newline_533: .asciz "\n"
lit_535: .asciz "| For Tipo While           | "
space_536: .asciz " "
int_buffer_536: .space 20
space_538: .asciz " "
lit_539: .asciz "    | 4     |"
newline_539: .asciz "\n"
lit_541: .asciz "| For Clásico              | "
space_542: .asciz " "
int_buffer_542: .space 20
space_544: .asciz " "
lit_545: .asciz "    | 3     |"
newline_545: .asciz "\n"
lit_547: .asciz "| For Range                | "
space_548: .asciz " "
int_buffer_548: .space 20
space_550: .asciz " "
lit_551: .asciz "    | 3     |"
newline_551: .asciz "\n"
lit_553: .asciz "| Switch/Case              | "
space_554: .asciz " "
int_buffer_554: .space 20
space_556: .asciz " "
lit_557: .asciz "    | 3     |"
newline_557: .asciz "\n"
lit_559: .asciz "| Break                    | "
space_560: .asciz " "
int_buffer_560: .space 20
space_562: .asciz " "
lit_563: .asciz "    | 3     |"
newline_563: .asciz "\n"
lit_565: .asciz "| Continue                 | "
space_566: .asciz " "
int_buffer_566: .space 20
space_568: .asciz " "
lit_569: .asciz "    | 3     |"
newline_569: .asciz "\n"
lit_571: .asciz "+--------------------------+--------+-------+"
newline_571: .asciz "\n"
lit_573: .asciz "| TOTAL                    | "
space_574: .asciz " "
int_buffer_574: .space 20
space_576: .asciz " "
lit_577: .asciz "   | 25    |"
newline_577: .asciz "\n"
lit_579: .asciz "+--------------------------+--------+-------+"
newline_579: .asciz "\n"

// ARM64 Assembly
.text
.global _start
_start:
    stp x29, x30, [sp, #-16]!
    mov x29, sp
    sub sp, sp, #512

    // Declaraciones de variables
    // Debug: Símbolos en tabla: 53
    // Símbolo: '1389', Tipo: 'Función', Dato: 'void'

    // Declaración de variable: main
    // Símbolo: '1392', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntos
    mov x9, #0
    str x9, [sp, #16]
    // Símbolo: '1395', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosEntornos
    mov x9, #0
    str x9, [sp, #24]
    // Símbolo: '1398', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: b
    mov x9, #10
    str x9, [sp, #32]
    // Símbolo: '1401', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: b
    mov x9, #20
    str x9, [sp, #32]
    // Símbolo: '1404', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: c
    mov x9, #10
    str x9, [sp, #40]
    // Símbolo: '1407', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: d
    mov x9, #10
    str x9, [sp, #48]
    // Símbolo: '1410', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: d
    mov x9, #20
    str x9, [sp, #48]
    // Símbolo: '1413', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosIfElse
    mov x9, #0
    str x9, [sp, #56]
    // Símbolo: '1416', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosForWhile
    mov x9, #0
    str x9, [sp, #64]
    // Símbolo: '1419', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1422', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: suma
    mov x9, #0
    str x9, [sp, #80]
    // Símbolo: '1425', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1428', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: producto
    mov x9, #1
    str x9, [sp, #96]
    // Símbolo: '1431', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: n
    mov x9, #5
    str x9, [sp, #104]
    // Símbolo: '1434', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: x
    mov x9, #0
    str x9, [sp, #112]
    // Símbolo: '1437', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1440', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: fila
    adr x9, str_0
    str x9, [sp, #120]
    // Símbolo: '1443', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1446', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: fila
    adr x9, str_1
    str x9, [sp, #120]
    // Símbolo: '1449', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1452', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: fila
    adr x9, str_2
    str x9, [sp, #120]
    // Símbolo: '1455', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1458', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: fila
    adr x9, str_3
    str x9, [sp, #120]
    // Símbolo: '1461', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #0
    str x9, [sp, #88]
    // Símbolo: '1464', Tipo: 'Variable', Dato: 'string'

    // Declaración de variable: fila
    adr x9, str_4
    str x9, [sp, #120]
    // Símbolo: '1467', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosForClasico
    mov x9, #0
    str x9, [sp, #128]
    // Símbolo: '1470', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1473', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1476', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #1
    str x9, [sp, #72]
    // Símbolo: '1479', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #1
    str x9, [sp, #72]
    // Símbolo: '1482', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1485', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1488', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1491', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1494', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1497', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: j
    mov x9, #1
    str x9, [sp, #88]
    // Símbolo: '1500', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosForRange
    mov x9, #0
    str x9, [sp, #136]
    // Símbolo: '1503', Tipo: 'Variable', Dato: '[]int'

    // Declaración de variable: numeros
    // Símbolo: '1506', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: sumaIndices
    mov x9, #0
    str x9, [sp, #144]
    // Símbolo: '1519', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosSwitch
    mov x9, #0
    str x9, [sp, #152]
    // Símbolo: '1522', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: dia
    mov x9, #1
    str x9, [sp, #160]
    // Símbolo: '1525', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: numero
    mov x9, #100
    str x9, [sp, #168]
    // Símbolo: '1528', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: numeroBreak
    mov x9, #2
    str x9, [sp, #176]
    // Símbolo: '1531', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosBreak
    mov x9, #0
    str x9, [sp, #184]
    // Símbolo: '1534', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: contador
    mov x9, #0
    str x9, [sp, #192]
    // Símbolo: '1537', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1540', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1543', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: puntosContinue
    mov x9, #0
    str x9, [sp, #200]
    // Símbolo: '1546', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: impares
    mov x9, #0
    str x9, [sp, #208]
    // Símbolo: '1549', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: pares
    mov x9, #0
    str x9, [sp, #216]
    // Símbolo: '1552', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]
    // Símbolo: '1555', Tipo: 'Variable', Dato: 'int'

    // Declaración de variable: i
    mov x9, #0
    str x9, [sp, #72]

    // Procesando instrucciones

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \=\=\= Archivo de prueba de funcionalidades intermedias \=\=\=
    adr x0, lit_6
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_6
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_6
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_6
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Validaciones manuales esperadas: 4
    adr x0, lit_8
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_8
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_8
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_8
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \=\=\=\= Manejo de entornos \=\=\=\=
    adr x0, lit_10
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_10
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_10
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_10
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nVariable redeclarada en un entorno diferente
    adr x0, lit_12
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_12
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_12
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_12
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_13
    // Bloque if (then)
    b endif_13
else_13:
endif_13:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: b dentro del if \=
    adr x0, lit_15
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_15
    mov x8, #64
    svc #0
    adr x0, space_16
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_16
    mov x8, #64
    svc #0
    // Imprimir entero: b
    ldr x0, [sp, #32]
    adr x1, int_buffer_16
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_16
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_17
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_17
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: b==20
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_18
    // Bloque if (then)
    b endif_18
else_18:
    // Bloque else
endif_18:

    // Procesando nodo: Condicion  Valor=b==20

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosEntornos (int) en [sp, #24]
    mov x9, #2
    str x9, [sp, #24]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK Redeclaración en entorno diferente: correcto
    adr x0, lit_20
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_20
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_20
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_20
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: b fuera del if \=
    adr x0, lit_22
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_22
    mov x8, #64
    svc #0
    adr x0, space_23
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_23
    mov x8, #64
    svc #0
    // Imprimir entero: b
    ldr x0, [sp, #32]
    adr x1, int_buffer_23
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_23
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_24
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_24
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nUso de variable en un entorno superior
    adr x0, lit_26
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_26
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_26
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_26
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_27
    // Bloque if (then)
    b endif_27
else_27:
endif_27:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: c dentro del if \=
    adr x0, lit_29
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_29
    mov x8, #64
    svc #0
    adr x0, space_30
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_30
    mov x8, #64
    svc #0
    // Imprimir entero: c
    ldr x0, [sp, #40]
    adr x1, int_buffer_30
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_30
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_31
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_31
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: d dentro del if \=
    adr x0, lit_33
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_33
    mov x8, #64
    svc #0
    adr x0, space_34
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_34
    mov x8, #64
    svc #0
    // Imprimir entero: d
    ldr x0, [sp, #48]
    adr x1, int_buffer_34
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_34
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_35
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_35
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 30 a c (int) en [sp, #40]
    mov x9, #30
    str x9, [sp, #40]

    // Procesando nodo: Asignacion  Valor=
    // Asignando 30 a d (int) en [sp, #48]
    mov x9, #30
    str x9, [sp, #48]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: c fuera del if \=
    adr x0, lit_37
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_37
    mov x8, #64
    svc #0
    adr x0, space_38
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_38
    mov x8, #64
    svc #0
    // Imprimir entero: c
    ldr x0, [sp, #40]
    adr x1, int_buffer_38
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_38
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_39
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_39
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: d fuera del if \=
    adr x0, lit_41
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_41
    mov x8, #64
    svc #0
    adr x0, space_42
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_42
    mov x8, #64
    svc #0
    // Imprimir entero: d
    ldr x0, [sp, #48]
    adr x1, int_buffer_42
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_42
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_43
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_43
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: c==30&&d==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_44
    // Bloque if (then)
    b endif_44
else_44:
    // Bloque else
endif_44:

    // Procesando nodo: Condicion  Valor=c==30&&d==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosEntornos (int) en [sp, #24]
    mov x9, #3
    str x9, [sp, #24]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK Uso de variable en entorno superior: correcto
    adr x0, lit_46
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_46
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_46
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_46
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= If / Else \=\=\=\=
    adr x0, lit_48
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_48
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_48
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_48
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_50
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_50
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_50
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_50
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: If simple
    adr x0, lit_52
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_52
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_52
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_52
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_53
    // Bloque if (then)
    b endif_53
else_53:
endif_53:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Condición verdadera
    adr x0, lit_55
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_55
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_55
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_55
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a puntosIfElse (int) en [sp, #56]
    mov x9, #1
    str x9, [sp, #56]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nIf-Else
    adr x0, lit_57
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_57
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_57
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_57
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_58
    // Bloque if (then)
    b endif_58
else_58:
    // Bloque else
endif_58:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Condición verdadera en if-else
    adr x0, lit_60
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_60
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_60
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_60
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: false
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_61
    // Bloque if (then)
    b endif_61
else_61:
    // Bloque else
endif_61:

    // Procesando nodo: Condicion  Valor=false

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Condición falsa, ejecutando else
    adr x0, lit_63
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_63
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_63
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_63
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosIfElse (int) en [sp, #56]
    mov x9, #2
    str x9, [sp, #56]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nIf-ElseIf-Else
    adr x0, lit_65
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_65
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_65
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_65
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_66
    // Bloque if (then)
    b endif_66
else_66:
    // Bloque else
endif_66:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Primera condición verdadera
    adr x0, lit_68
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_68
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_68
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_68
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: false
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_69
    // Bloque if (then)
    b endif_69
else_69:
    // Bloque else
endif_69:

    // Procesando nodo: Condicion  Valor=false

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: true
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_70
    // Bloque if (then)
    b endif_70
else_70:
    // Bloque else
endif_70:

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Segunda condición verdadera
    adr x0, lit_72
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_72
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_72
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_72
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosIfElse (int) en [sp, #56]
    mov x9, #3
    str x9, [sp, #56]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_74
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_74
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_74
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_74
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= For Tipo While \=\=\=\=
    adr x0, lit_76
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_76
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_76
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_76
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: For como while simple
    adr x0, lit_78
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_78
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_78
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_78
    mov x8, #64
    svc #0

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=i<5

    // Procesando nodo: Expresion  Valor=i<5

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_80
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_80
    mov x8, #64
    svc #0
    adr x0, space_81
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_81
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_81
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_81
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_82
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_82
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a i (int) en [sp, #72]
    mov x9, #1
    str x9, [sp, #72]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_84
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_84
    mov x8, #64
    svc #0
    adr x0, space_85
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_85
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_85
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_85
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_86
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_86
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a suma (int) en [sp, #80]
    mov x9, #1
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a i (int) en [sp, #72]
    mov x9, #2
    str x9, [sp, #72]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_88
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_88
    mov x8, #64
    svc #0
    adr x0, space_89
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_89
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_89
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_89
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_90
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_90
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a suma (int) en [sp, #80]
    mov x9, #3
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a i (int) en [sp, #72]
    mov x9, #3
    str x9, [sp, #72]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_92
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_92
    mov x8, #64
    svc #0
    adr x0, space_93
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_93
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_93
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_93
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_94
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_94
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a suma (int) en [sp, #80]
    mov x9, #6
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a i (int) en [sp, #72]
    mov x9, #4
    str x9, [sp, #72]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_96
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_96
    mov x8, #64
    svc #0
    adr x0, space_97
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_97
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_97
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_97
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_98
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_98
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a suma (int) en [sp, #80]
    mov x9, #10
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a i (int) en [sp, #72]
    mov x9, #5
    str x9, [sp, #72]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: suma==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_99
    // Bloque if (then)
    b endif_99
else_99:
    // Bloque else
endif_99:

    // Procesando nodo: Condicion  Valor=suma==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a puntosForWhile (int) en [sp, #64]
    mov x9, #1
    str x9, [sp, #64]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For como while simple: correcto
    adr x0, lit_101
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_101
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_101
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_101
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nFor como while con condición compuesta
    adr x0, lit_103
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_103
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_103
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_103
    mov x8, #64
    svc #0

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<5&&producto<50

    // Procesando nodo: Expresion  Valor=j<5&&producto<50

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a producto (int) en [sp, #96]
    mov x9, #1
    str x9, [sp, #96]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: j \=
    adr x0, lit_105
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_105
    mov x8, #64
    svc #0
    adr x0, space_106
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_106
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_106
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_106
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_108
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: producto \=
    adr x0, lit_109
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_109
    mov x8, #64
    svc #0
    adr x0, space_110
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_110
    mov x8, #64
    svc #0
    // Imprimir entero: producto
    ldr x0, [sp, #96]
    adr x1, int_buffer_110
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_110
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_111
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_111
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a producto (int) en [sp, #96]
    mov x9, #2
    str x9, [sp, #96]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: j \=
    adr x0, lit_113
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_113
    mov x8, #64
    svc #0
    adr x0, space_114
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_114
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_114
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_114
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_116
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: producto \=
    adr x0, lit_117
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_117
    mov x8, #64
    svc #0
    adr x0, space_118
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_118
    mov x8, #64
    svc #0
    // Imprimir entero: producto
    ldr x0, [sp, #96]
    adr x1, int_buffer_118
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_118
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_119
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_119
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a producto (int) en [sp, #96]
    mov x9, #6
    str x9, [sp, #96]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: j \=
    adr x0, lit_121
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_121
    mov x8, #64
    svc #0
    adr x0, space_122
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_122
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_122
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_122
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_124
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: producto \=
    adr x0, lit_125
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_125
    mov x8, #64
    svc #0
    adr x0, space_126
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_126
    mov x8, #64
    svc #0
    // Imprimir entero: producto
    ldr x0, [sp, #96]
    adr x1, int_buffer_126
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_126
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_127
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_127
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 24 a producto (int) en [sp, #96]
    mov x9, #24
    str x9, [sp, #96]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: j \=
    adr x0, lit_129
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_129
    mov x8, #64
    svc #0
    adr x0, space_130
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_130
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_130
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_130
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_132
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: producto \=
    adr x0, lit_133
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_133
    mov x8, #64
    svc #0
    adr x0, space_134
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_134
    mov x8, #64
    svc #0
    // Imprimir entero: producto
    ldr x0, [sp, #96]
    adr x1, int_buffer_134
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_134
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_135
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_135
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: Asignacion  Valor=
    // Asignando 120 a producto (int) en [sp, #96]
    mov x9, #120
    str x9, [sp, #96]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: j \=
    adr x0, lit_137
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_137
    mov x8, #64
    svc #0
    adr x0, space_138
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_138
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_138
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_138
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_140
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: producto \=
    adr x0, lit_141
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_141
    mov x8, #64
    svc #0
    adr x0, space_142
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_142
    mov x8, #64
    svc #0
    // Imprimir entero: producto
    ldr x0, [sp, #96]
    adr x1, int_buffer_142
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_142
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_143
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_143
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: j==5&&producto==120
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_144
    // Bloque if (then)
    b endif_144
else_144:
    // Bloque else
endif_144:

    // Procesando nodo: Condicion  Valor=j==5&&producto==120

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosForWhile (int) en [sp, #64]
    mov x9, #2
    str x9, [sp, #64]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For como while con condición compuesta: correcto
    adr x0, lit_146
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_146
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_146
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_146
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nFor como while anidado (patrón X)
    adr x0, lit_148
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_148
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_148
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_148
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_150
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_150
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_150
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_150
    mov x8, #64
    svc #0

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=x<n

    // Procesando nodo: Expresion  Valor=x<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<n

    // Procesando nodo: Expresion  Valor=j<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_151
    // Bloque if (then)
    b endif_151
else_151:
    // Bloque else
endif_151:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando * a fila (string) en [sp, #120]
    adr x0, str_152
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_153
    // Bloque if (then)
    b endif_153
else_153:
    // Bloque else
endif_153:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *  a fila (string) en [sp, #120]
    adr x0, str_154
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_155
    // Bloque if (then)
    b endif_155
else_155:
    // Bloque else
endif_155:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *   a fila (string) en [sp, #120]
    adr x0, str_156
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_157
    // Bloque if (then)
    b endif_157
else_157:
    // Bloque else
endif_157:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *    a fila (string) en [sp, #120]
    adr x0, str_158
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_159
    // Bloque if (then)
    b endif_159
else_159:
    // Bloque else
endif_159:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *   * a fila (string) en [sp, #120]
    adr x0, str_160
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir string: fila
    ldr x0, [sp, #120]
    cmp x0, #0
    beq skip_print_161
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #120]
    mov x8, #64
    svc #0
    b end_print_161
skip_print_161:
    adr x0, empty_str_161
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_161
    mov x8, #64
    svc #0
end_print_161:
    // Imprimir salto de línea
    adr x0, newline_162
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_162
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a x (int) en [sp, #112]
    mov x9, #1
    str x9, [sp, #112]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<n

    // Procesando nodo: Expresion  Valor=j<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_163
    // Bloque if (then)
    b endif_163
else_163:
    // Bloque else
endif_163:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   a fila (string) en [sp, #120]
    adr x0, str_164
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_165
    // Bloque if (then)
    b endif_165
else_165:
    // Bloque else
endif_165:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * a fila (string) en [sp, #120]
    adr x0, str_166
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_167
    // Bloque if (then)
    b endif_167
else_167:
    // Bloque else
endif_167:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  *  a fila (string) en [sp, #120]
    adr x0, str_168
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_169
    // Bloque if (then)
    b endif_169
else_169:
    // Bloque else
endif_169:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * * a fila (string) en [sp, #120]
    adr x0, str_170
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_171
    // Bloque if (then)
    b endif_171
else_171:
    // Bloque else
endif_171:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * *  a fila (string) en [sp, #120]
    adr x0, str_172
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir string: fila
    ldr x0, [sp, #120]
    cmp x0, #0
    beq skip_print_173
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #120]
    mov x8, #64
    svc #0
    b end_print_173
skip_print_173:
    adr x0, empty_str_173
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_173
    mov x8, #64
    svc #0
end_print_173:
    // Imprimir salto de línea
    adr x0, newline_174
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_174
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a x (int) en [sp, #112]
    mov x9, #2
    str x9, [sp, #112]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<n

    // Procesando nodo: Expresion  Valor=j<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_175
    // Bloque if (then)
    b endif_175
else_175:
    // Bloque else
endif_175:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   a fila (string) en [sp, #120]
    adr x0, str_176
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_177
    // Bloque if (then)
    b endif_177
else_177:
    // Bloque else
endif_177:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando    a fila (string) en [sp, #120]
    adr x0, str_178
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_179
    // Bloque if (then)
    b endif_179
else_179:
    // Bloque else
endif_179:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   * a fila (string) en [sp, #120]
    adr x0, str_180
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_181
    // Bloque if (then)
    b endif_181
else_181:
    // Bloque else
endif_181:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   *  a fila (string) en [sp, #120]
    adr x0, str_182
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_183
    // Bloque if (then)
    b endif_183
else_183:
    // Bloque else
endif_183:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   *   a fila (string) en [sp, #120]
    adr x0, str_184
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir string: fila
    ldr x0, [sp, #120]
    cmp x0, #0
    beq skip_print_185
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #120]
    mov x8, #64
    svc #0
    b end_print_185
skip_print_185:
    adr x0, empty_str_185
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_185
    mov x8, #64
    svc #0
end_print_185:
    // Imprimir salto de línea
    adr x0, newline_186
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_186
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a x (int) en [sp, #112]
    mov x9, #3
    str x9, [sp, #112]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<n

    // Procesando nodo: Expresion  Valor=j<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_187
    // Bloque if (then)
    b endif_187
else_187:
    // Bloque else
endif_187:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando   a fila (string) en [sp, #120]
    adr x0, str_188
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_189
    // Bloque if (then)
    b endif_189
else_189:
    // Bloque else
endif_189:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * a fila (string) en [sp, #120]
    adr x0, str_190
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_191
    // Bloque if (then)
    b endif_191
else_191:
    // Bloque else
endif_191:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  *  a fila (string) en [sp, #120]
    adr x0, str_192
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_193
    // Bloque if (then)
    b endif_193
else_193:
    // Bloque else
endif_193:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * * a fila (string) en [sp, #120]
    adr x0, str_194
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_195
    // Bloque if (then)
    b endif_195
else_195:
    // Bloque else
endif_195:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando  * *  a fila (string) en [sp, #120]
    adr x0, str_196
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir string: fila
    ldr x0, [sp, #120]
    cmp x0, #0
    beq skip_print_197
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #120]
    mov x8, #64
    svc #0
    b end_print_197
skip_print_197:
    adr x0, empty_str_197
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_197
    mov x8, #64
    svc #0
end_print_197:
    // Imprimir salto de línea
    adr x0, newline_198
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_198
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a x (int) en [sp, #112]
    mov x9, #4
    str x9, [sp, #112]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=j<n

    // Procesando nodo: Expresion  Valor=j<n

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_199
    // Bloque if (then)
    b endif_199
else_199:
    // Bloque else
endif_199:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando * a fila (string) en [sp, #120]
    adr x0, str_200
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a j (int) en [sp, #88]
    mov x9, #1
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_201
    // Bloque if (then)
    b endif_201
else_201:
    // Bloque else
endif_201:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *  a fila (string) en [sp, #120]
    adr x0, str_202
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a j (int) en [sp, #88]
    mov x9, #2
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_203
    // Bloque if (then)
    b endif_203
else_203:
    // Bloque else
endif_203:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *   a fila (string) en [sp, #120]
    adr x0, str_204
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a j (int) en [sp, #88]
    mov x9, #3
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_205
    // Bloque if (then)
    b endif_205
else_205:
    // Bloque else
endif_205:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *    a fila (string) en [sp, #120]
    adr x0, str_206
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a j (int) en [sp, #88]
    mov x9, #4
    str x9, [sp, #88]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=-

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==j||x+j==n-1
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_207
    // Bloque if (then)
    b endif_207
else_207:
    // Bloque else
endif_207:

    // Procesando nodo: Condicion  Valor=x==j||x+j==n-1

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando *   * a fila (string) en [sp, #120]
    adr x0, str_208
    str x0, [sp, #120]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a j (int) en [sp, #88]
    mov x9, #5
    str x9, [sp, #88]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir string: fila
    ldr x0, [sp, #120]
    cmp x0, #0
    beq skip_print_209
    bl string_length
    mov x2, x0
    mov x0, #1
    ldr x1, [sp, #120]
    mov x8, #64
    svc #0
    b end_print_209
skip_print_209:
    adr x0, empty_str_209
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, empty_str_209
    mov x8, #64
    svc #0
end_print_209:
    // Imprimir salto de línea
    adr x0, newline_210
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_210
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a x (int) en [sp, #112]
    mov x9, #5
    str x9, [sp, #112]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_212
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_212
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_212
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_212
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: x==5
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_213
    // Bloque if (then)
    b endif_213
else_213:
    // Bloque else
endif_213:

    // Procesando nodo: Condicion  Valor=x==5

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a puntosForWhile (int) en [sp, #64]
    mov x9, #4
    str x9, [sp, #64]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For como while anidado: correcto
    adr x0, lit_215
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_215
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_215
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_215
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= For Clásico \=\=\=\=
    adr x0, lit_217
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_217
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_217
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_217
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: For clásico simple
    adr x0, lit_219
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_219
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_219
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_219
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=i:=0

    // Procesando nodo: Condicion  Valor=i<5

    // Procesando nodo: Expresion  Valor=i<5

    // Procesando nodo: Incremento  Valor=i++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_221
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_221
    mov x8, #64
    svc #0
    adr x0, space_222
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_222
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_222
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_222
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_223
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_223
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_225
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_225
    mov x8, #64
    svc #0
    adr x0, space_226
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_226
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_226
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_226
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_227
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_227
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a suma (int) en [sp, #80]
    mov x9, #1
    str x9, [sp, #80]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_229
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_229
    mov x8, #64
    svc #0
    adr x0, space_230
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_230
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_230
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_230
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_231
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_231
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a suma (int) en [sp, #80]
    mov x9, #3
    str x9, [sp, #80]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_233
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_233
    mov x8, #64
    svc #0
    adr x0, space_234
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_234
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_234
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_234
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_235
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_235
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a suma (int) en [sp, #80]
    mov x9, #6
    str x9, [sp, #80]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_237
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_237
    mov x8, #64
    svc #0
    adr x0, space_238
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_238
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_238
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_238
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_239
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_239
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a suma (int) en [sp, #80]
    mov x9, #10
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: suma==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_240
    // Bloque if (then)
    b endif_240
else_240:
    // Bloque else
endif_240:

    // Procesando nodo: Condicion  Valor=suma==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a puntosForClasico (int) en [sp, #128]
    mov x9, #1
    str x9, [sp, #128]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For clásico simple: correcto
    adr x0, lit_242
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_242
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_242
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_242
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nFor clásico anidado (tabla de multiplicar)
    adr x0, lit_244
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_244
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_244
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_244
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_246
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_246
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_246
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_246
    mov x8, #64
    svc #0

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=i:=1

    // Procesando nodo: Condicion  Valor=i<=3

    // Procesando nodo: Expresion  Valor=i<=3

    // Procesando nodo: Incremento  Valor=i++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=j:=1

    // Procesando nodo: Condicion  Valor=j<=3

    // Procesando nodo: Expresion  Valor=j<=3

    // Procesando nodo: Incremento  Valor=j++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_247
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_247
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_249
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_249
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_249
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_251
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_251
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_251
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_253
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_254
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_254
    mov x8, #64
    svc #0
    adr x0, space_255
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_255
    mov x8, #64
    svc #0
    // Imprimir literal: 1
    adr x0, lit_256
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_256
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_256
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_256
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_257
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_257
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_259
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_259
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_259
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_261
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_261
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_261
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_263
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_264
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_264
    mov x8, #64
    svc #0
    adr x0, space_265
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_265
    mov x8, #64
    svc #0
    // Imprimir literal: 2
    adr x0, lit_266
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_266
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_266
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_266
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_267
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_267
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_269
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_269
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_269
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_271
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_271
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_271
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_273
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_274
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_274
    mov x8, #64
    svc #0
    adr x0, space_275
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_275
    mov x8, #64
    svc #0
    // Imprimir literal: 3
    adr x0, lit_276
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_276
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_276
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_276
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=
    // Imprimir salto de línea
    adr x0, newline_277
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_277
    mov x8, #64
    svc #0

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=j:=1

    // Procesando nodo: Condicion  Valor=j<=3

    // Procesando nodo: Expresion  Valor=j<=3

    // Procesando nodo: Incremento  Valor=j++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_278
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_278
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_280
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_280
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_280
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_282
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_282
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_282
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_284
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_285
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_285
    mov x8, #64
    svc #0
    adr x0, space_286
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_286
    mov x8, #64
    svc #0
    // Imprimir literal: 2
    adr x0, lit_287
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_287
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_287
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_287
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_288
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_288
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_290
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_290
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_290
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_292
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_292
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_292
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_294
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_295
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_295
    mov x8, #64
    svc #0
    adr x0, space_296
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_296
    mov x8, #64
    svc #0
    // Imprimir literal: 4
    adr x0, lit_297
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_297
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_297
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_297
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_298
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_298
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_300
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_300
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_300
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_302
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_302
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_302
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_304
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_305
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_305
    mov x8, #64
    svc #0
    adr x0, space_306
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_306
    mov x8, #64
    svc #0
    // Imprimir literal: 6
    adr x0, lit_307
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_307
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_307
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_307
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=
    // Imprimir salto de línea
    adr x0, newline_308
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_308
    mov x8, #64
    svc #0

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=j:=1

    // Procesando nodo: Condicion  Valor=j<=3

    // Procesando nodo: Expresion  Valor=j<=3

    // Procesando nodo: Incremento  Valor=j++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_309
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_309
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_311
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_311
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_311
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_313
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_313
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_313
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_315
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_316
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_316
    mov x8, #64
    svc #0
    adr x0, space_317
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_317
    mov x8, #64
    svc #0
    // Imprimir literal: 3
    adr x0, lit_318
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_318
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_318
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_318
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_319
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_319
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_321
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_321
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_321
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_323
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_323
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_323
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_325
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_326
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_326
    mov x8, #64
    svc #0
    adr x0, space_327
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_327
    mov x8, #64
    svc #0
    // Imprimir literal: 6
    adr x0, lit_328
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_328
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_328
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_328
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=*

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_329
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_329
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_331
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: x
    ldr x0, [sp, #112]
    adr x1, int_buffer_331
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_331
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_333
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir entero: j
    ldr x0, [sp, #88]
    adr x1, int_buffer_333
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_333
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_335
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_336
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_336
    mov x8, #64
    svc #0
    adr x0, space_337
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_337
    mov x8, #64
    svc #0
    // Imprimir literal: 9
    adr x0, lit_338
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_338
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_338
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_338
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=
    // Imprimir salto de línea
    adr x0, newline_339
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_339
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_341
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_341
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_341
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_341
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosForClasico (int) en [sp, #128]
    mov x9, #3
    str x9, [sp, #128]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For clásico anidado: correcto
    adr x0, lit_343
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_343
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_343
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_343
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= For Range \=\=\=\=
    adr x0, lit_345
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_345
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_345
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_345
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: For range con slice
    adr x0, lit_347
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_347
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_347
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_347
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: ForRangeLoop  Valor=

    // Procesando nodo: Iterador  Valor=i

    // Procesando nodo: Variable  Valor=valor

    // Procesando nodo: Coleccion  Valor=numeros

    // Procesando nodo: Expresion  Valor=numeros

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Índice
    adr x0, lit_349
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_349
    mov x8, #64
    svc #0
    adr x0, space_350
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_350
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_350
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_350
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_352
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_353
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_353
    mov x8, #64
    svc #0
    adr x0, space_354
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_354
    mov x8, #64
    svc #0
    // Imprimir literal: valor
    adr x0, lit_355
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_355
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_355
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_355
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a suma (int) en [sp, #80]
    mov x9, #10
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a sumaIndices (int) en [sp, #144]
    mov x9, #0
    str x9, [sp, #144]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Índice
    adr x0, lit_357
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_357
    mov x8, #64
    svc #0
    adr x0, space_358
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_358
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_358
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_358
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_360
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_361
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_361
    mov x8, #64
    svc #0
    adr x0, space_362
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_362
    mov x8, #64
    svc #0
    // Imprimir literal: valor
    adr x0, lit_363
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_363
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_363
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_363
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 30 a suma (int) en [sp, #80]
    mov x9, #30
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a sumaIndices (int) en [sp, #144]
    mov x9, #1
    str x9, [sp, #144]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Índice
    adr x0, lit_365
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_365
    mov x8, #64
    svc #0
    adr x0, space_366
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_366
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_366
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_366
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_368
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_369
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_369
    mov x8, #64
    svc #0
    adr x0, space_370
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_370
    mov x8, #64
    svc #0
    // Imprimir literal: valor
    adr x0, lit_371
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_371
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_371
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_371
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 60 a suma (int) en [sp, #80]
    mov x9, #60
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a sumaIndices (int) en [sp, #144]
    mov x9, #3
    str x9, [sp, #144]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Índice
    adr x0, lit_373
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_373
    mov x8, #64
    svc #0
    adr x0, space_374
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_374
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_374
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_374
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_376
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_377
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_377
    mov x8, #64
    svc #0
    adr x0, space_378
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_378
    mov x8, #64
    svc #0
    // Imprimir literal: valor
    adr x0, lit_379
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_379
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_379
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_379
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 100 a suma (int) en [sp, #80]
    mov x9, #100
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a sumaIndices (int) en [sp, #144]
    mov x9, #6
    str x9, [sp, #144]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Índice
    adr x0, lit_381
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_381
    mov x8, #64
    svc #0
    adr x0, space_382
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_382
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_382
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_382
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_384
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal: \=
    adr x0, lit_385
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_385
    mov x8, #64
    svc #0
    adr x0, space_386
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_386
    mov x8, #64
    svc #0
    // Imprimir literal: valor
    adr x0, lit_387
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_387
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_387
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_387
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 150 a suma (int) en [sp, #80]
    mov x9, #150
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a sumaIndices (int) en [sp, #144]
    mov x9, #10
    str x9, [sp, #144]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: suma==150
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_388
    // Bloque if (then)
    b endif_388
else_388:
    // Bloque else
endif_388:

    // Procesando nodo: Condicion  Valor=suma==150

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosForRange (int) en [sp, #136]
    mov x9, #2
    str x9, [sp, #136]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For range con slice: correcto
    adr x0, lit_390
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_390
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_390
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_390
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: sumaIndices==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_391
    // Bloque if (then)
    b endif_391
else_391:
    // Bloque else
endif_391:

    // Procesando nodo: Condicion  Valor=sumaIndices==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosForRange (int) en [sp, #136]
    mov x9, #3
    str x9, [sp, #136]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK For range con índices: correcto
    adr x0, lit_393
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_393
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_393
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_393
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= Switch/Case \=\=\=\=
    adr x0, lit_395
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_395
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_395
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_395
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\n###Validacion Manual
    adr x0, lit_397
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_397
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_397
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_397
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Switch simple
    adr x0, lit_399
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_399
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_399
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_399
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Lunes
    adr x0, lit_401
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_401
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_401
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_401
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a puntosSwitch (int) en [sp, #152]
    mov x9, #1
    str x9, [sp, #152]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nSwitch con default
    adr x0, lit_403
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_403
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_403
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_403
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Número no reconocido, se ejecuta default
    adr x0, lit_405
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_405
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_405
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_405
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosSwitch (int) en [sp, #152]
    mov x9, #2
    str x9, [sp, #152]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nSwitch con break explícito
    adr x0, lit_407
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_407
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_407
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_407
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Caso 2 - Se ejecuta este y debe detenerse
    adr x0, lit_409
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_409
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_409
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_409
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosSwitch (int) en [sp, #152]
    mov x9, #3
    str x9, [sp, #152]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: 
    adr x0, lit_411
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_411
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_411
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_411
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= Break \=\=\=\=
    adr x0, lit_413
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_413
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_413
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_413
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Break en for infinito
    adr x0, lit_415
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_415
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_415
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_415
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=true

    // Procesando nodo: Expresion  Valor=true

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: contador \=
    adr x0, lit_417
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_417
    mov x8, #64
    svc #0
    adr x0, space_418
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_418
    mov x8, #64
    svc #0
    // Imprimir entero: contador
    ldr x0, [sp, #192]
    adr x1, int_buffer_418
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_418
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_419
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_419
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a contador (int) en [sp, #192]
    mov x9, #1
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador>=5
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_420
    // Bloque if (then)
    b endif_420
else_420:
endif_420:

    // Procesando nodo: Condicion  Valor=contador>=5

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: contador \=
    adr x0, lit_422
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_422
    mov x8, #64
    svc #0
    adr x0, space_423
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_423
    mov x8, #64
    svc #0
    // Imprimir entero: contador
    ldr x0, [sp, #192]
    adr x1, int_buffer_423
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_423
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_424
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_424
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a suma (int) en [sp, #80]
    mov x9, #1
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a contador (int) en [sp, #192]
    mov x9, #2
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador>=5
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_425
    // Bloque if (then)
    b endif_425
else_425:
endif_425:

    // Procesando nodo: Condicion  Valor=contador>=5

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: contador \=
    adr x0, lit_427
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_427
    mov x8, #64
    svc #0
    adr x0, space_428
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_428
    mov x8, #64
    svc #0
    // Imprimir entero: contador
    ldr x0, [sp, #192]
    adr x1, int_buffer_428
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_428
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_429
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_429
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a suma (int) en [sp, #80]
    mov x9, #3
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a contador (int) en [sp, #192]
    mov x9, #3
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador>=5
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_430
    // Bloque if (then)
    b endif_430
else_430:
endif_430:

    // Procesando nodo: Condicion  Valor=contador>=5

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: contador \=
    adr x0, lit_432
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_432
    mov x8, #64
    svc #0
    adr x0, space_433
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_433
    mov x8, #64
    svc #0
    // Imprimir entero: contador
    ldr x0, [sp, #192]
    adr x1, int_buffer_433
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_433
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_434
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_434
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a suma (int) en [sp, #80]
    mov x9, #6
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a contador (int) en [sp, #192]
    mov x9, #4
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador>=5
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_435
    // Bloque if (then)
    b endif_435
else_435:
endif_435:

    // Procesando nodo: Condicion  Valor=contador>=5

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: contador \=
    adr x0, lit_437
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_437
    mov x8, #64
    svc #0
    adr x0, space_438
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_438
    mov x8, #64
    svc #0
    // Imprimir entero: contador
    ldr x0, [sp, #192]
    adr x1, int_buffer_438
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_438
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_439
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_439
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a suma (int) en [sp, #80]
    mov x9, #10
    str x9, [sp, #80]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a contador (int) en [sp, #192]
    mov x9, #5
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador>=5
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_440
    // Bloque if (then)
    b endif_440
else_440:
endif_440:

    // Procesando nodo: Condicion  Valor=contador>=5

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: suma==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_441
    // Bloque if (then)
    b endif_441
else_441:
    // Bloque else
endif_441:

    // Procesando nodo: Condicion  Valor=suma==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a puntosBreak (int) en [sp, #184]
    mov x9, #1
    str x9, [sp, #184]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK Break en for infinito: correcto
    adr x0, lit_443
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_443
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_443
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_443
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nBreak en for clásico
    adr x0, lit_445
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_445
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_445
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_445
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=i:=0

    // Procesando nodo: Condicion  Valor=i<10

    // Procesando nodo: Expresion  Valor=i<10

    // Procesando nodo: Incremento  Valor=i++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_447
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_447
    mov x8, #64
    svc #0
    adr x0, space_448
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_448
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_448
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_448
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_449
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_449
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a suma (int) en [sp, #80]
    mov x9, #0
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i>=4
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_450
    // Bloque if (then)
    b endif_450
else_450:
endif_450:

    // Procesando nodo: Condicion  Valor=i>=4

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_452
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_452
    mov x8, #64
    svc #0
    adr x0, space_453
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_453
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_453
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_453
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_454
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_454
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a suma (int) en [sp, #80]
    mov x9, #1
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i>=4
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_455
    // Bloque if (then)
    b endif_455
else_455:
endif_455:

    // Procesando nodo: Condicion  Valor=i>=4

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_457
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_457
    mov x8, #64
    svc #0
    adr x0, space_458
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_458
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_458
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_458
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_459
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_459
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a suma (int) en [sp, #80]
    mov x9, #3
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i>=4
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_460
    // Bloque if (then)
    b endif_460
else_460:
endif_460:

    // Procesando nodo: Condicion  Valor=i>=4

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_462
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_462
    mov x8, #64
    svc #0
    adr x0, space_463
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_463
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_463
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_463
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_464
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_464
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a suma (int) en [sp, #80]
    mov x9, #6
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i>=4
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_465
    // Bloque if (then)
    b endif_465
else_465:
endif_465:

    // Procesando nodo: Condicion  Valor=i>=4

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: i \=
    adr x0, lit_467
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_467
    mov x8, #64
    svc #0
    adr x0, space_468
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_468
    mov x8, #64
    svc #0
    // Imprimir entero: i
    ldr x0, [sp, #72]
    adr x1, int_buffer_468
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_468
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_469
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_469
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a suma (int) en [sp, #80]
    mov x9, #10
    str x9, [sp, #80]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i>=4
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_470
    // Bloque if (then)
    b endif_470
else_470:
endif_470:

    // Procesando nodo: Condicion  Valor=i>=4

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: suma==10
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_471
    // Bloque if (then)
    b endif_471
else_471:
    // Bloque else
endif_471:

    // Procesando nodo: Condicion  Valor=suma==10

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a puntosBreak (int) en [sp, #184]
    mov x9, #3
    str x9, [sp, #184]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK Break en for clásico: correcto
    adr x0, lit_473
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_473
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_473
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_473
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\=\= Continue \=\=\=\=
    adr x0, lit_475
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_475
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_475
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_475
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Continue en for tipo while
    adr x0, lit_477
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_477
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_477
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_477
    mov x8, #64
    svc #0

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a contador (int) en [sp, #192]
    mov x9, #0
    str x9, [sp, #192]

    // Procesando nodo: ForLoop  Valor=

    // Procesando nodo: Condicion  Valor=contador<10

    // Procesando nodo: Expresion  Valor=contador<10

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a contador (int) en [sp, #192]
    mov x9, #1
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_478
    // Bloque if (then)
    b endif_478
else_478:
endif_478:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 1 a impares (int) en [sp, #208]
    mov x9, #1
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a contador (int) en [sp, #192]
    mov x9, #2
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_479
    // Bloque if (then)
    b endif_479
else_479:
endif_479:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a impares (int) en [sp, #208]
    mov x9, #3
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 3 a contador (int) en [sp, #192]
    mov x9, #3
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_480
    // Bloque if (then)
    b endif_480
else_480:
endif_480:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a impares (int) en [sp, #208]
    mov x9, #6
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 4 a contador (int) en [sp, #192]
    mov x9, #4
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_481
    // Bloque if (then)
    b endif_481
else_481:
endif_481:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a impares (int) en [sp, #208]
    mov x9, #10
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 5 a contador (int) en [sp, #192]
    mov x9, #5
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_482
    // Bloque if (then)
    b endif_482
else_482:
endif_482:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 15 a impares (int) en [sp, #208]
    mov x9, #15
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a contador (int) en [sp, #192]
    mov x9, #6
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_483
    // Bloque if (then)
    b endif_483
else_483:
endif_483:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 21 a impares (int) en [sp, #208]
    mov x9, #21
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 7 a contador (int) en [sp, #192]
    mov x9, #7
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_484
    // Bloque if (then)
    b endif_484
else_484:
endif_484:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 28 a impares (int) en [sp, #208]
    mov x9, #28
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 8 a contador (int) en [sp, #192]
    mov x9, #8
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_485
    // Bloque if (then)
    b endif_485
else_485:
endif_485:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 36 a impares (int) en [sp, #208]
    mov x9, #36
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 9 a contador (int) en [sp, #192]
    mov x9, #9
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_486
    // Bloque if (then)
    b endif_486
else_486:
endif_486:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 45 a impares (int) en [sp, #208]
    mov x9, #45
    str x9, [sp, #208]

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 10 a contador (int) en [sp, #192]
    mov x9, #10
    str x9, [sp, #192]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: contador%2==0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_487
    // Bloque if (then)
    b endif_487
else_487:
endif_487:

    // Procesando nodo: Condicion  Valor=contador%2==0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 55 a impares (int) en [sp, #208]
    mov x9, #55
    str x9, [sp, #208]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Números impares:
    adr x0, lit_489
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_489
    mov x8, #64
    svc #0
    adr x0, space_490
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_490
    mov x8, #64
    svc #0
    // Imprimir entero: impares
    ldr x0, [sp, #208]
    adr x1, int_buffer_490
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_490
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_491
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_491
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: impares==25
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_492
    // Bloque if (then)
    b endif_492
else_492:
    // Bloque else
endif_492:

    // Procesando nodo: Condicion  Valor=impares==25

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: X Continue en for tipo while: incorrecto
    adr x0, lit_494
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_494
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_494
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_494
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \nContinue en for clásico
    adr x0, lit_496
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_496
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_496
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_496
    mov x8, #64
    svc #0

    // Procesando nodo: ForClassicLoop  Valor=

    // Procesando nodo: Inicializacion  Valor=i:=0

    // Procesando nodo: Condicion  Valor=i<10

    // Procesando nodo: Expresion  Valor=i<10

    // Procesando nodo: Incremento  Valor=i++

    // Procesando nodo: BloqueFor  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_497
    // Bloque if (then)
    b endif_497
else_497:
endif_497:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 0 a pares (int) en [sp, #216]
    mov x9, #0
    str x9, [sp, #216]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_498
    // Bloque if (then)
    b endif_498
else_498:
endif_498:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_499
    // Bloque if (then)
    b endif_499
else_499:
endif_499:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a pares (int) en [sp, #216]
    mov x9, #2
    str x9, [sp, #216]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_500
    // Bloque if (then)
    b endif_500
else_500:
endif_500:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_501
    // Bloque if (then)
    b endif_501
else_501:
endif_501:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 6 a pares (int) en [sp, #216]
    mov x9, #6
    str x9, [sp, #216]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_502
    // Bloque if (then)
    b endif_502
else_502:
endif_502:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_503
    // Bloque if (then)
    b endif_503
else_503:
endif_503:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 12 a pares (int) en [sp, #216]
    mov x9, #12
    str x9, [sp, #216]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_504
    // Bloque if (then)
    b endif_504
else_504:
endif_504:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'False'
    mov x0, #0
    cmp x0, #0
    beq else_505
    // Bloque if (then)
    b endif_505
else_505:
endif_505:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=False

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 20 a pares (int) en [sp, #216]
    mov x9, #20
    str x9, [sp, #216]

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: i%2!=0
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_506
    // Bloque if (then)
    b endif_506
else_506:
endif_506:

    // Procesando nodo: Condicion  Valor=i%2!=0

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: Números pares:
    adr x0, lit_508
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_508
    mov x8, #64
    svc #0
    adr x0, space_509
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_509
    mov x8, #64
    svc #0
    // Imprimir entero: pares
    ldr x0, [sp, #216]
    adr x1, int_buffer_509
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_509
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_510
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_510
    mov x8, #64
    svc #0

    // Procesando nodo: IfStatement  Valor=

    // If Statement
    // Evaluar condición: pares==20
    // ADVERTENCIA: No se pudo evaluar condición 'True'
    mov x0, #0
    cmp x0, #0
    beq else_511
    // Bloque if (then)
    b endif_511
else_511:
    // Bloque else
endif_511:

    // Procesando nodo: Condicion  Valor=pares==20

    // Procesando nodo: Expresion  Valor=True

    // Procesando nodo: BloqueIf  Valor=

    // Procesando nodo: BloqueElse  Valor=

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 2 a puntosContinue (int) en [sp, #200]
    mov x9, #2
    str x9, [sp, #200]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: OK Continue en for clásico: correcto
    adr x0, lit_513
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_513
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_513
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_513
    mov x8, #64
    svc #0

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Operacion  Valor=+

    // Procesando nodo: Asignacion  Valor=
    // Asignando 24 a puntos (int) en [sp, #16]
    mov x9, #24
    str x9, [sp, #16]

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: \n\=\=\= Tabla de Resultados \=\=\=
    adr x0, lit_515
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_515
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_515
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_515
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: +--------------------------+--------+-------+
    adr x0, lit_517
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_517
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_517
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_517
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | Característica           | Puntos | Total |
    adr x0, lit_519
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_519
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_519
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_519
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: +--------------------------+--------+-------+
    adr x0, lit_521
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_521
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_521
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_521
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | Manejo de entornos       | 
    adr x0, lit_523
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_523
    mov x8, #64
    svc #0
    adr x0, space_524
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_524
    mov x8, #64
    svc #0
    // Imprimir entero: puntosEntornos
    ldr x0, [sp, #24]
    adr x1, int_buffer_524
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_524
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_526
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_527
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_527
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_527
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_527
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | If / Else                | 
    adr x0, lit_529
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_529
    mov x8, #64
    svc #0
    adr x0, space_530
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_530
    mov x8, #64
    svc #0
    // Imprimir entero: puntosIfElse
    ldr x0, [sp, #56]
    adr x1, int_buffer_530
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_530
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_532
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_533
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_533
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_533
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_533
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | For Tipo While           | 
    adr x0, lit_535
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_535
    mov x8, #64
    svc #0
    adr x0, space_536
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_536
    mov x8, #64
    svc #0
    // Imprimir entero: puntosForWhile
    ldr x0, [sp, #64]
    adr x1, int_buffer_536
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_536
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_538
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 4     |
    adr x0, lit_539
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_539
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_539
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_539
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | For Clásico              | 
    adr x0, lit_541
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_541
    mov x8, #64
    svc #0
    adr x0, space_542
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_542
    mov x8, #64
    svc #0
    // Imprimir entero: puntosForClasico
    ldr x0, [sp, #128]
    adr x1, int_buffer_542
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_542
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_544
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_545
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_545
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_545
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_545
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | For Range                | 
    adr x0, lit_547
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_547
    mov x8, #64
    svc #0
    adr x0, space_548
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_548
    mov x8, #64
    svc #0
    // Imprimir entero: puntosForRange
    ldr x0, [sp, #136]
    adr x1, int_buffer_548
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_548
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_550
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_551
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_551
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_551
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_551
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | Switch/Case              | 
    adr x0, lit_553
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_553
    mov x8, #64
    svc #0
    adr x0, space_554
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_554
    mov x8, #64
    svc #0
    // Imprimir entero: puntosSwitch
    ldr x0, [sp, #152]
    adr x1, int_buffer_554
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_554
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_556
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_557
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_557
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_557
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_557
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | Break                    | 
    adr x0, lit_559
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_559
    mov x8, #64
    svc #0
    adr x0, space_560
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_560
    mov x8, #64
    svc #0
    // Imprimir entero: puntosBreak
    ldr x0, [sp, #184]
    adr x1, int_buffer_560
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_560
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_562
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_563
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_563
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_563
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_563
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | Continue                 | 
    adr x0, lit_565
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_565
    mov x8, #64
    svc #0
    adr x0, space_566
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_566
    mov x8, #64
    svc #0
    // Imprimir entero: puntosContinue
    ldr x0, [sp, #200]
    adr x1, int_buffer_566
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_566
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_568
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:     | 3     |
    adr x0, lit_569
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_569
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_569
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_569
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: +--------------------------+--------+-------+
    adr x0, lit_571
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_571
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_571
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_571
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: | TOTAL                    | 
    adr x0, lit_573
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_573
    mov x8, #64
    svc #0
    adr x0, space_574
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, space_574
    mov x8, #64
    svc #0
    // Imprimir entero: puntos
    ldr x0, [sp, #16]
    adr x1, int_buffer_574
    bl int_to_string
    mov x2, x0
    mov x0, #1
    adr x1, int_buffer_574
    mov x8, #64
    svc #0
    mov x0, #1
    adr x1, space_576
    mov x2, #1
    mov x8, #64
    svc #0
    // Imprimir literal:    | 25    |
    adr x0, lit_577
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_577
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_577
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_577
    mov x8, #64
    svc #0

    // Procesando nodo: fmt.Println  Valor=

    // fmt.Println
    // Imprimir literal: +--------------------------+--------+-------+
    adr x0, lit_579
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, lit_579
    mov x8, #64
    svc #0
    // Imprimir salto de línea
    adr x0, newline_579
    bl string_length
    mov x2, x0
    mov x0, #1
    adr x1, newline_579
    mov x8, #64
    svc #0

    // Saltar directamente al final del programa
    b program_exit

// Rutinas auxiliares

string_copy:
    // x0 = destino, x1 = origen
    cmp x1, #0
    beq string_copy_null
    mov x2, x0
string_copy_loop:
    ldrb w3, [x1], #1
    strb w3, [x0], #1
    cmp w3, #0
    bne string_copy_loop
    sub x0, x0, #1
    ret
string_copy_null:
    // Si origen es NULL, simplemente poner terminador nulo en destino
    mov w3, #0
    strb w3, [x0]
    ret
int_to_string:
    // x0 = número, x1 = buffer
    mov x9, x0        // Guardar número original
    mov x10, x1       // Guardar dirección original del buffer
    mov x11, #0       // Contador de dígitos
    mov x12, #0       // Para contar dígitos totales
    mov x14, #0       // Flag para indicar si es negativo
    cmp x9, #0
    bne count_digits
    mov w2, #48       // '0'
    strb w2, [x10]
    mov w2, #0        // Null-terminator
    strb w2, [x10, #1]
    mov x0, #1
    ret
    // Verificar si es negativo
    cmp x9, #0
    bge positive_number
    // Es negativo, imprimir '-'
    neg x9, x9     // Convertir a positivo
    mov w2, #45    // '-' en ASCII
    strb w2, [x10], #1
    mov x14, #1    // Flag para indicar que es negativo (afectará a la longitud final)
positive_number:
count_digits:
    mov x2, x9        // Copiar número
    mov x13, #10      // Constante 10 para división
digit_count_loop:
    cbz x2, gen_digits
    udiv x2, x2, x13  // Dividir por 10 usando registro
    add x12, x12, #1  // Incrementar contador
    b digit_count_loop
gen_digits:
    mov x11, x12      // Longitud total
    mov x2, #1        // Potencia de 10
    mov x13, #10      // Base 10
calc_power_loop:
    cmp x12, #1       // Si solo queda 1 dígito, salir
    ble extract_digits
    mul x2, x2, x13   // Multiplicar por 10
    sub x12, x12, #1  // Decrementar contador
    b calc_power_loop
extract_digits:
    mov x3, x9        // Restaurar número
    mov x12, x11      // Restaurar contador total
extract_loop:
    cbz x12, finish_str
    udiv x4, x3, x2   // x4 = número / potencia
    msub x3, x4, x2, x3 // x3 = x3 - (x4 * x2) (resto)
    add w4, w4, #48   // Convertir a ASCII
    strb w4, [x10], #1// Almacenar y avanzar
    udiv x2, x2, x13  // Reducir potencia por 10 (x13=10)
    sub x12, x12, #1  // Decrementar contador
    b extract_loop
finish_str:
    mov w2, #0        // Null-terminator
    strb w2, [x10]
    // Si era negativo, incrementar longitud por el signo
    cbz x14, return_length
    add x11, x11, #1  // Ajustar longitud por el signo '-'
return_length:
    mov x0, x11       // Retornar longitud
    ret

string_compare:
    // x0 = primera cadena, x1 = segunda cadena
    // Devuelve 0 si son iguales, no-0 si son diferentes
    // Verificar si alguna es NULL
    cmp x0, #0
    beq check_second_null
    cmp x1, #0
    beq strings_not_equal
    // Ambas no son NULL, comparar
compare_loop:
    ldrb w2, [x0], #1
    ldrb w3, [x1], #1
    cmp w2, w3
    bne strings_not_equal
    cbz w2, strings_equal
    b compare_loop
check_second_null:
    cmp x1, #0
    beq strings_equal
strings_not_equal:
    mov x0, #1
    ret
strings_equal:
    mov x0, #0
    ret

string_length:
    // x0 = dirección de la cadena
    cmp x0, #0
    beq string_length_null
    mov x2, x0
length_loop:
    ldrb w1, [x0], #1  // cargar byte y avanzar
    cmp w1, #0         // comparar con null
    bne length_loop    // continuar si no es null
    sub x0, x0, x2     // calcular longitud
    sub x0, x0, #1     // ajustar por null
    ret
string_length_null:
    mov x0, #0         // Retornar 0 para cadenas NULL
    ret

float_to_string:
    // Implementación mejorada
    stp x29, x30, [sp, #-16]!
    mov x29, sp
    stp x19, x20, [sp, #-16]!
    mov x19, x0                // Buffer address

    // Extraer signo
    mov x21, #0               // Flag para negativo
    fcmpe d0, #0.0
    bpl convert_integer
    mov x21, #1               // Es negativo
    fneg d0, d0               // Convertir a positivo
    mov w1, #45               // '-'
    strb w1, [x19], #1
convert_integer:
    // Convertir parte entera
    fcvtzs x0, d0             // Convertir a entero
    mov x1, x19               // Buffer para la parte entera
    bl int_to_string          // Convertir entero
    mov x20, x0               // Guardar longitud
    cmp x21, #1               // Si era negativo
    beq was_negative
    add x19, x19, x0          // Avanzar buffer
    b add_decimal
was_negative:
    add x19, x19, x0          // Avanzar buffer
    add x20, x20, #1          // +1 por el signo
add_decimal:
    // Añadir punto
    mov w1, #'.'
    strb w1, [x19], #1
    add x20, x20, #1

    // Convertir 6 dígitos decimales
    fcvtzs x0, d0             // Volver a convertir a entero
    scvtf d1, x0              // Convertir entero a float
    fsub d1, d0, d1           // Parte decimal = float - entero
    mov x22, #2               // 6 dígitos decimales
    fmov d2, #10.0            // Constante 10.0
decimal_loop:
    fmul d1, d1, d2           // d1 = d1 * 10
    fcvtzs x0, d1             // Entero = int(d1)
    add w0, w0, #48           // Convertir a ASCII
    strb w0, [x19], #1        // Guardar y avanzar
    add x20, x20, #1          // Incrementar longitud
    scvtf d3, x0              // Convertir dígito a flotante
    mov x0, #48               // Valor ASCII '0'
    scvtf d4, x0              // Convertir 48 a float
    fsub d3, d3, d4           // Restar ASCII offset
    fsub d1, d1, d3           // Restar dígito
    subs x22, x22, #1         // Decrementar contador
    bne decimal_loop          // Continuar si no hemos terminado
    // Terminar cadena
    mov w1, #0
    strb w1, [x19]

    // Retornar longitud total
    mov x0, x20
    ldp x19, x20, [sp], #16
    ldp x29, x30, [sp], #16
    ret

program_exit:
    // Terminar programa
    mov sp, x29
    ldp x29, x30, [sp], #16
    mov x8, #93     // exit syscall
    mov x0, #0      // exit code 0
    svc #0          // supervisor call
